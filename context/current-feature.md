# Current Feature: Favorites Page Sorting

## Status

In Progress

## Goals

- Add client-side sorting controls to `/favorites` (`FavoritesList.tsx`)
- Support sorting by: Name, Date, and Item Type
- Sorting applies to the Items section; Collections have no item type, so their sort control is limited to Name/Date (or a separate decision made during implementation)

## Notes

- `FavoritesList.tsx` is a `"use client"` component already receiving `items: FavoriteItem[]` and `collections: FavoriteCollection[]` as props from the server (`getFavoriteItems`/`getFavoriteCollections`, both ordered by `updatedAt desc` server-side) — sorting should be done client-side over these already-fetched arrays, no new server/DB work needed
- "Date" should likely sort by `createdAt` (the field currently displayed in each row via `formatDate`), not the server's `updatedAt` ordering
- "Item Type" sorts by `item.type.name`
- Decided (explicit user choice via AskUserQuestion before implementation): sorting applies to the Items section only — Collections keeps its current server order (updatedAt desc), no sort control added there since collections have no item type
- Decided: sort UI is a `Select` (Name/Date/Type) next to an asc/desc toggle button
- Decided: default sort on load is Date, newest first — matches the existing server-side order so the page looks unchanged until the user picks a different sort

## History

- 2026-07-15: Phase 1 (ShadCN setup, /dashboard route, top bar, sidebar/main placeholders) implemented and verified on branch `feature/dashboard-phase-1`. Committed and merged.
- 2026-07-16: Phase 2 (collapsible sidebar with item-type links, favorite/all collections, user avatar footer, desktop collapse + mobile drawer toggle) implemented on branch `feature/dashboard-phase-2`. Verified in-browser (desktop collapse, section collapse, mobile drawer) with no console errors; `npm run build` and `npm run lint` both pass. Committed and merged; branch deleted (local + remote).
- 2026-07-17: Phase 3 (stats cards, recent collections, pinned items, 10 recent items; stats card icon colors aligned to the existing collection/type palette) implemented on branch `feature/dashboard-phase-3`. Verified in-browser with no console errors; `npm run build` and `npm run lint` both pass. Committed and merged; branch deleted (local + remote). Dashboard UI 3-phase build complete.
- 2026-07-20/21: Prisma 7 + Neon PostgreSQL setup implemented on branch `feature/prisma-neon-setup`. Hit and resolved a real blocker: Prisma 7 requires Node 20.19+/22.12+/24+, machine had v20.15.1 — installed current Node (v26.5.0) via Homebrew with user approval. Added `prisma.config.ts` (Prisma 7's config format, loading `.env` then `.env.local`), `prisma/schema.prisma` (full data model from project-overview.md plus NextAuth's `Account`/`Session`/`VerificationToken`, with explicit cascade rules and indexes on all FK/lookup columns), `src/lib/prisma.ts` (client singleton using `@prisma/adapter-neon` with the dev-mode global-singleton guard), and `.env.example`/`.gitignore` updates. Set `package.json` `"type": "module"` (required by Prisma 7's ESM-only client). Ran `prisma migrate dev --name init` against the real dev Neon branch (not `db push`) — created `prisma/migrations/20260720213954_init/`, all 9 tables confirmed, `prisma migrate status` reports in sync. `npm run build` and `npm run lint` both pass. Ready to commit and merge.
- 2026-07-21: Added `scripts/test-db.ts` (standalone connectivity check, `npm run db:test`) and `prisma/seed.ts` (per @context/features/seed-spec.md, copied in from course resources) on the same `feature/prisma-neon-setup` branch. Seed creates the demo user (bcryptjs-hashed password), 7 system item types, and 5 collections with 18 items (real URLs for link items); deletes-then-recreates the demo user + system types first, so `npm run db:seed` is safe to re-run — verified by running it twice with identical output. Ran against the real dev Neon branch and confirmed via `db:test` (User count: 1). `npm run build` and `npm run lint` both pass. Ready to commit and merge.
- 2026-07-21: Extended `scripts/test-db.ts` to fetch and print the seeded demo data — system item type count, then the demo user with each collection and its items (grouped by item type). Verified against the real dev Neon branch: prints all 7 item types, 5 collections, and 18 items correctly. `npm run build` and `npm run lint` both pass.
- 2026-07-21/22: Dashboard Collections (per @context/features/dashboard-collections-spec.md) implemented on branch `feature/dashboard-collections`. Added `src/lib/db/collections.ts` (`getRecentCollections`, hardcoded to the demo user like the existing scripts since auth isn't wired up yet) computing per-collection item count, border color (from the most-used `ItemType.color` among its items), and the deduped list of item types present. Updated `RecentCollections.tsx` (now an async server component) and `CollectionCard.tsx` to consume this instead of `src/lib/mock-data.ts`. Fixed a pre-existing `icon-map.ts` gap where the seeded snippet type's icon name (`"Code"`) had no entry (only `"Code2"` did), which would've silently fallen back to a generic folder icon. Wrapped the fetch in a try/catch so a DB failure renders a visible `text-destructive` error message instead of crashing the page — verified by forcing a temporary throw and confirming the error rendered, then confirming normal data rendered again after reverting. Also caught and reverted an experimental `swr`/client-fetching detour that broke the build (RSC doesn't export `useSWR`); removed the `swr` dependency since it ended up unused. Verified in-browser: all 5 real collections render with correct item counts, type-derived border colors, and correct type icons. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Ready to commit and merge.
- 2026-07-22: Dashboard Items (per @context/features/dashboard-items-spec.md) implemented on branch `feature/dashboard-items`. Added `src/lib/db/items.ts` (`getPinnedItems`, `getRecentItems`, `getItemStats`, all demo-user-scoped like `collections.ts`), mapping each item's `type` relation and joined `tags` (via `ItemTag`) into a flat `ItemSummary` shape. Added `getCollectionStats` to the existing `src/lib/db/collections.ts` for real collection/favorite counts. Updated `PinnedItems.tsx`, `RecentItems.tsx`, and `StatsCards.tsx` to async server components consuming these instead of `src/lib/mock-data.ts`, with the same try/catch → `text-destructive` error-message pattern used for collections. Updated `ItemRow.tsx` to take the DB-shaped `ItemSummary` (type/tags no longer looked up from mock arrays; `createdAt` is now a `Date`). Left `src/lib/mock-data.ts` and `Sidebar.tsx` (still consumes it for nav/filters) untouched — out of scope for this spec. Verified in-browser via the seeded demo data: all 18 real items render in Recent Items with correct titles, descriptions, dates, and type-derived border colors/icons; Pinned section correctly renders nothing since no seeded items are pinned; stats cards show real counts (18 items, 5 collections, 0/0 favorites, matching the seed which sets no favorites/pins). `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Ready to commit and merge.
- 2026-07-22/23: Stats & Sidebar (per @context/features/stats-sidebar-spec.md) implemented on branch `feature/stats-sidebar`. Added `getItemTypes()` to `src/lib/db/items.ts` (real per-type item counts for the demo user, sorted to a fixed display order — Snippets, Prompts, Commands, Notes, Files, Images, Links — instead of alphabetical) and a new `src/lib/db/user.ts` (`getCurrentUser`) so the sidebar's avatar/footer also comes from the DB instead of `mock-data.ts`. Rewrote `Sidebar.tsx` to take `itemTypes`/`collections`/`currentUser` as props: type links go to `/items/[typename]`, non-favorite collections show a colored circle (from `borderColor`) instead of an item count, added a "View all collections" link to `/collections`, and the whole Collections section (header/list/link) is skipped when there are no collections — Types nav and the footer stay visible regardless. Added `SidebarContainer.tsx`, an async server component fetching all three with the same try/catch → `text-destructive` error pattern used elsewhere, and changed `DashboardShell.tsx` to accept a `sidebar` ReactNode prop (rendered in both the desktop aside and mobile sheet) instead of importing `Sidebar` directly, since a `"use client"` file can't import and render a Server Component — `dashboard/page.tsx` now passes `<SidebarContainer />` in. Went down a client-side `useSWR` + `/api/sidebar` route detour (the same mistake as the earlier Collections feature) which broke the Turbopack build (`chunking context does not support external modules`) because `DashboardShell` was directly rendering the Server Component; reverted `SidebarContainer`/`DashboardShell`/`page.tsx` back to the server-fetch version, but kept the now-unused `src/app/api/sidebar/route.ts` and the `swr` dependency in place per explicit request. Updated `prisma/seed.ts` to mark the "React Patterns" and "Terminal Commands" collections `isFavorite: true` (all 5 seeded collections previously had `isFavorite: false`, so the sidebar's Favorites section was correctly empty, not a bug) and the "useDebounce Hook" item `isFavorite: true` (0 favorite items previously existed, so the Favorite Items stat showed 0); re-ran `npm run db:seed` against the dev Neon DB after each change and confirmed via direct DB queries. Verified in-browser/via rendered HTML: real item-type counts in the fixed order with correct `/items/[type]` links, `/collections` link present, two collections rendering under FAVORITES with the star icon and the rest with colored circles, real "Demo User"/`demo@devstash.io` in the footer, and the Favorite Items stat reflecting the 1 seeded favorite. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Not yet committed/merged.
- 2026-07-28: Add Pro Badge to Sidebar (per @context/features/add-pro-badge-sidebar.md) implemented on branch `feature/add-pro-badge-sidebar`. Added an `outline`-variant ShadCN `Badge` (uppercase "Pro") in `Sidebar.tsx`, rendered inline next to the type label only when `type.name === "file" || type.name === "image"`, signaling those two item types are Pro-tier. No new components/props — a conditional render inside the existing per-type-row loop. Verified via rendered dashboard HTML: badge markup and classes (outline variant, uppercase) present on exactly the File and Image rows and no others; in-app browser screenshot wasn't possible (no display access in this sandbox), so verification relied on the rendered HTML output. `npm run build` and `npm run lint` both pass. Committed and merged into `main` (per explicit choice over `development`, since the branch was cut from `main`); local branch deleted.
- 2026-07-28: Housekeeping cleanup (`/cleanup`). Deleted `src/lib/mock-data.ts` (no remaining imports — all dashboard components had already migrated to real DB data across earlier phases). Staged the already-on-disk deletion of `src/app/api/sidebar/route.ts`, which supersedes the 07-22/23 note above that it was being kept — it and its only consumer (a client-side `useSWR` detour) are gone, so removed the now-unused `swr` dependency via `npm uninstall swr`. Also fixed `.env.production`'s `DATABASE_URL` line, which had stray whitespace around the key/`=` (` DATABASE_URL = ...`) unlike `.env`/`.env.example`. Confirmed no `console.log`, stale TODOs, or `@ts-ignore` in `src/`, and `npm run lint` reports no unused imports. Not yet committed.
- 2026-07-28: Fix: centralize hardcoded demo-user email (raised by the codebase-scanner subagent) implemented on branch `fix/centralize-demo-user`. Added `src/lib/demo-user.ts` exporting `DEMO_USER_EMAIL` with an explicit `// TODO(auth)` comment explaining every DB helper scopes to this single user until NextAuth session lookup is wired up. Updated `src/lib/db/collections.ts`, `src/lib/db/items.ts`, and `src/lib/db/user.ts` to import it instead of each independently declaring its own local `DEMO_EMAIL` constant. No behavior change. `prisma/seed.ts` and `scripts/test-db.ts` keep their own local `DEMO_EMAIL` (not production code paths, out of scope). Verified: `npm run build`, `npm run lint`, and `tsc --noEmit` all pass; started the dev server and confirmed `/dashboard` still renders the seeded demo user's data (`demo@devstash.io` / "Demo User") correctly. Committed and merged into `development`; branch deleted.
- 2026-07-28: Fix: over-fetching in `getRecentCollections` (raised by the codebase-scanner subagent) implemented on branch `fix/collections-overfetch`. Changed the Prisma query in `src/lib/db/collections.ts` from `include: { items: { include: { type: true } } }` to a scoped `select` (collection's `id`/`name`/`description`/`isFavorite`, and per-item only `typeId` plus `type`'s `id`/`name`/`icon`/`color`), so it no longer pulls full `Item` rows (content, description, fileUrl, url, etc.) just to compute an item count and border color. No behavior change — same `CollectionSummary[]` output. Noted `getRecentCollections` was already wrapped in React's `cache()` in the working tree by the time this fix started (addressing the separate duplicate-query finding) — left untouched, not part of this fix. Verified: `npm run build`, `npm run lint`, and `tsc --noEmit` all pass; started the dev server and confirmed `/dashboard` renders real seeded collections (React Patterns, Terminal Commands) with no error output. Not yet committed/merged.
- 2026-07-29/30: Auth Phase 1 — NextAuth v5 + GitHub OAuth (per @context/features/auth-phase-1-spec.md) implemented on branch `feature/auth-phase-1`. Installed `next-auth@beta` (5.0.0-beta.32) and `@auth/prisma-adapter`; the `Account`/`Session`/`VerificationToken` models already existed in `prisma/schema.prisma` from the earlier Prisma setup, so no new migration was needed. Added `src/app/api/auth/[...nextauth]/route.ts`, `src/proxy.ts` (protects `/dashboard/:path*`, redirects unauthenticated requests to `/api/auth/signin` with `callbackUrl` set to the original destination), and `src/types/next-auth.d.ts` (extends `Session.user` with `id`). Final `src/auth.ts` is a single `NextAuth()` instance (`PrismaAdapter`, GitHub provider, implicit `database` session strategy) rather than the spec's split `auth.config.ts`/`auth.ts` + forced-`jwt` pattern — deviated deliberately after checking Prisma's own official Auth.js+Next.js guide, which uses the same un-split, database-session shape; reasoned this project's `@prisma/adapter-neon` (HTTP-based, unlike a typical TCP Postgres driver) makes the edge-runtime constraint that motivates the split largely moot here. `src/auth.config.ts` was deleted once `proxy.ts` switched to importing the real `auth` directly instead of a second lightweight instance. A `/do-i-understand` review (run twice, across the initial jwt/split-config version and the later single-instance/database-strategy version) caught two real bugs before merge: (1) the sign-in redirect never set `callbackUrl`, so users landed on `/` instead of back on `/dashboard` after GitHub OAuth; (2) a `session` callback that spread the full Prisma `User` row (or, later, the raw pre-callback `session` object) onto `session.user` — twice, once during manual edits and once in an initial attempt at the database-strategy fix — leaking `password`, `stripeCustomerId`, and `stripeSubscriptionId` into the client-visible session. Fixed by having the `session` callback explicitly construct `{ expires, user: { id, name, email, image } }` by field name rather than spreading. Verified live end-to-end multiple times against the real GitHub OAuth app and the real dev Neon DB: unauthenticated `/dashboard` redirects to sign-in with correct `callbackUrl`, GitHub OAuth completes and lands back on `/dashboard`, `/api/auth/session` returns exactly `{ expires, user: { id, name, email, image } }` (confirmed via direct `Session`/`User` table queries that database-strategy sessions are genuinely being created), and sign-out re-locks `/dashboard`. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Committed and merged into `main`; branch deleted.
- 2026-07-30/08-03: Auth Phase 2 — Email/Password Credentials Provider (per @context/features/auth-phase-2-spec.md) implemented on branch `feature/auth-phase-2`. Added a `Credentials` provider to `src/auth.ts` (bcryptjs-verified `authorize` against the existing `User.password` field, Zod-validated via new `src/lib/validations/auth.ts`) alongside the existing `GitHub` provider, and `POST /api/auth/register` (`src/app/api/auth/register/route.ts`) for sign-up — validates input, checks for an existing user, hashes with bcryptjs (12 rounds, matching `prisma/seed.ts`), returns `{ success, data/error }`. Installed `zod` (v4; used the new top-level `z.email()` form per current docs) since the coding standards require Zod validation and it wasn't yet a dependency. Had to switch `session` from the implicit `database` strategy (phase 1's deliberate choice) to `session: { strategy: "jwt" }` — confirmed via Auth.js docs this is a hard requirement, not a style choice: a Credentials sign-in never creates an adapter `Account`/`Session` row, so database-strategy sessions silently fail to establish after a successful credentials login. Rewrote the `jwt`/`session` callbacks around `token` instead of `user`, keeping the same explicit-field-construction discipline (no spreading) that phase 1 required for the leak fix. Added `token.id` via `next-auth/jwt` module augmentation in `src/types/next-auth.d.ts` — needed a same-named unused `JWT` import for TS to actually merge the augmentation (confirmed by removing it and watching `tsc` fail); the import is real, not dead code. Considered switching to Better Auth mid-task (its `emailAndPassword`/`socialProviders` don't have this JWT-vs-database fork) but stayed on NextAuth since phase 1 is already merged and working. A `/do-i-understand` review flagged two gaps that were _not_ fixed in this phase, deliberately left as tracked follow-ups rather than blocking merge: (1) no session-revocation path exists yet — JWT sessions are stateless, so there's currently no way to force-expire a specific user's session before natural token expiry; author described a token-version-on-User-model comparison as the intended fix but confirmed it isn't built. (2) `authorize()` collapses every failure mode (unknown email, GitHub-only account with no password, wrong password) into one generic `CredentialsSignin` error — author confirmed this wasn't deliberate and agreed a GitHub-only user deserves a better message than a generic invalid-credentials error, but that's not implemented yet either. Verified live end-to-end against the real dev Neon DB (test user created via curl, then deleted after verification): registration happy path plus 409/400 edge cases (duplicate email, password mismatch, too-short password, invalid email), credentials sign-in producing a valid `{id,name,email,image}`-only session, wrong password correctly rejected with no session created, `/dashboard` 200 with a valid session / 307 redirect without one. GitHub OAuth itself was **not** re-verified live — this environment's `.env` has no `GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET` — but the `GitHub` provider registration was untouched by this change. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Committed and merged into `main`; branch deleted. **Follow-ups to pick up in a later phase:** (a) build the token-version session-revocation check, (b) give GitHub-only users a distinct error when they try email/password sign-in.
- 2026-08-03/05: Auth Phase 3 — Sign In, Register & Sign Out UI (per @context/features/auth-phase-3-spec.md) implemented on branch `feature/auth-phase-3`. Added custom `src/app/sign-in/page.tsx` and `src/app/register/page.tsx` (replacing NextAuth's built-in pages, set via `pages: { signIn: "/sign-in" }` in `src/auth.ts`), `src/components/auth/SignInForm.tsx` (credentials + GitHub via Server Actions in `src/actions/auth.ts`, `useActionState` for pending/error state) and `RegisterForm.tsx` (client-side Zod validation, posts to the existing `/api/auth/register`, redirects to `/sign-in` on success with a `sonner` success toast — installed `sonner` and mounted `<Toaster>` in `src/app/layout.tsx` since no toast infra existed yet). Added `src/components/shared/UserAvatar.tsx` (GitHub image or initials fallback) and wired it into `Sidebar.tsx`'s footer as a dropdown-menu trigger (email label + "Sign out" form action) with the existing settings icon repurposed as a `/profile` link (new placeholder page, added to `proxy.ts`'s protected matcher). Switched `getCurrentUser()` (`src/lib/db/user.ts`) from the hardcoded demo-user lookup to the real session via `auth()`, adding `image` — a deliberate scope call so the sidebar identity reflects whoever is actually signed in; other DB helpers (`items.ts`/`collections.ts`) were left on the demo-user hardcode, unchanged. Hit and fixed two real bugs after initial implementation looked done: (1) a Turbopack dev-mode crash (`the chunking context does not support external modules (request: node:module)`) because `Sidebar.tsx` (a Client Component) imported `signOutAction` directly from `src/actions/auth.ts`, which pulls in `@/auth`'s `PrismaAdapter`/Prisma runtime — fixed by having only Server Components (`SidebarContainer.tsx`, `sign-in/page.tsx`) import from `@/actions/auth`, passing the bound actions down as props instead, the same pattern this project already uses for `DashboardShell`'s `sidebar` prop; (2) a live `OAuthAccountNotLinked` error traced to Auth.js's own account-linking guard — clicking "Sign in with GitHub" while already signed in (e.g. a stale session in another tab) attempts to link that GitHub account to whichever user is currently signed in rather than treating it as a fresh sign-in, and rejects since the GitHub account belongs to a different user; fixed by having `/sign-in` and `/register` redirect an already-authenticated visitor straight to `/dashboard?notice=already-signed-in` (small inline banner) before they can reach the GitHub button, rather than loosening `allowDangerousEmailAccountLinking`. Also fixed two Base UI-specific issues surfaced in-browser (`nativeButton={false}` on the Link-rendering profile button; wrapped the sign-out `DropdownMenuLabel` in `DropdownMenuGroup`), and applied `rounded-[10px]` to the sign-in card's buttons and email/password inputs per explicit request. Verified live end-to-end multiple times against the real dev Neon DB and real GitHub OAuth app across several fresh dev-server/cache restarts: `/sign-in` and `/register` render; GitHub OAuth completes and lands on `/dashboard` with the sidebar showing the real name/email/avatar image; credentials sign-in/sign-out work; register's password-mismatch validation and success-toast-then-redirect both confirmed; already-signed-in redirect confirmed on both `/sign-in` and `/register`; `/profile` renders protected with no console errors. Created and deleted several throwaway test users (`playwright-test@devstash.io`, `toast-test*@devstash.io`) from the dev Neon DB during verification, each with explicit confirmation before deletion. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Committed and merged into `main`; branch deleted. **Follow-up to pick up later:** the two auth-phase-2 follow-ups (token-version session revocation, distinct error for GitHub-only users on credentials sign-in) are still open.
- 2026-08-06: Email Verification on Register implemented on branch `feature/email-verification-register`. Added `src/lib/resend.ts` (client) and `src/lib/email.ts` (`sendVerificationEmail`, from `onboarding@resend.dev`) plus `src/lib/db/verification-tokens.ts`, which reuses the existing `VerificationToken` table (`identifier` = email) rather than adding a new model — tokens are SHA-256 hashed before storage, 24h TTL, single-use (deleted on consume), and a fresh request invalidates any prior token for that email. `POST /api/auth/register` now issues + sends a verification email after user creation, catching send failures so registration still succeeds if Resend has a hiccup. Decided (explicit user choice, offered as a question before implementation) that unverified users are **blocked** from credentials sign-in rather than let in with a banner: `src/auth.ts`'s `authorize()` now throws a custom `EmailNotVerifiedError extends CredentialsSignin` when `emailVerified` is null; GitHub OAuth is unaffected (already verified by GitHub). `src/actions/auth.ts` catches that error and surfaces `unverified`/`email` on `SignInActionResult`, and adds `resendVerificationEmail(email)` — deliberately returns `{success:true}` for both a nonexistent email and a GitHub-only account (no password) to avoid leaking account existence. `SignInForm.tsx` shows the blocked-sign-in message with a "Resend verification email" button when applicable. Added `src/app/verify-email/page.tsx` (server component; consumes the token, updates `User.emailVerified`, and renders success/expired/invalid states, with an inline server-action form to resend on the expired case). Verified live end-to-end against the real dev Neon DB: direct DB-level test of token creation/hashing/single-use consumption/expiry (temp script, deleted after); confirmed via a direct Resend API call that this environment's Resend account (registered to lydietoussaint123@gmail.com) is in test mode and can only deliver to its own address — a known dev limitation, not a bug, and confirmed the 403 is caught gracefully rather than crashing registration or the resend action; registered a real test user via curl, generated its token directly (bypassing email delivery) and hit `/verify-email` to confirm `emailVerified` gets set and the token becomes single-use (revisiting shows "Invalid verification link"); via Playwright, confirmed a verified user can sign in to `/dashboard` and a second, unverified user is blocked with the resend button, which itself degrades gracefully on the expected 403. Both throwaway test users deleted afterward with explicit confirmation. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Also added `scripts/delete-non-demo-users.ts` (`npm run db:delete-non-demo-users`, dry-run by default, `--confirm` to execute) as a separate ad hoc utility, committed separately from the feature — deletes every user except `demo@devstash.io` and relies on the schema's cascade rules to remove their content; used live to clean up test/personal accounts that had accumulated in the dev DB. Deleted `.playwright-mcp/` local test-tool output and added it to `.gitignore`. Committed as two focused commits and merged into `main`; branch deleted. **Note for later:** `.env.example` has never actually been tracked in this repo — it's been silently caught by the blanket `.env*` gitignore rule since the original Prisma/Neon setup phase, so the `RESEND_API_KEY` placeholder added here (and everything previously believed to be in `.env.example`) only exists on disk, not in version control; worth a deliberate `.gitignore` fix (e.g. `!.env.example`) in a future session.
- 2026-08-06: A `/do-i-understand` review of the email-verification feature surfaced two real gaps before they'd been touched by hand: (1) `/verify-email` consumes the token as a side effect of a plain GET, which email link-scanners/prefetchers can trigger and burn before the real user clicks — fix (a two-step confirm page) deliberately deferred, tracked as a follow-up, not built this session; (2) the register route's silent email-send failure creates a genuine dead end (account created, `emailVerified` stuck null, resend hits the same failure) — directly motivated the next feature below. Separately, `.env.example` was deleted outright (confirmed unused at runtime and never actually tracked in git) in favor of documenting required env vars directly in `CLAUDE.md`.
- 2026-08-10: Email Verification Toggle implemented on branch `feature/email-verification-toggle`, closing the register-dead-end gap from the review above. Added `src/lib/feature-flags.ts` (`isEmailVerificationEnabled()`, `EMAIL_VERIFICATION_ENABLED` env var, default enabled unless explicitly `"false"`) as the single source of truth, read by the register route, `authorize()`, and `resendVerificationEmail`. Decided (explicit user choice, offered as a question before implementation) that while disabled, `User.emailVerified` is auto-set at registration time rather than left `null`, so re-enabling the flag later doesn't retroactively lock out anyone who registered during the disabled period; the register route also skips sending entirely when disabled (no doomed Resend call, no orphaned `VerificationToken` row), and `resendVerificationEmail` no-ops. Documented the new var in `CLAUDE.md`. While debugging a separately-reported issue ("the confirmation toast doesn't appear after registering"), spent a long diagnostic detour chasing several wrong leads — a stale long-running dev server (killed and restarted; process had been running continuously since 2026-08-05), `.next` Turbopack cache corruption from interleaved `dev`/`build` runs (cleared, didn't fix it), and `reactCompiler: true` in `next.config.ts` (temporarily disabled to test, reverted after — not the cause) — before finding the real cause was a stale Playwright browser tab in the testing setup itself, confirmed by reproducing cleanly in a fresh tab. Kept one genuine improvement found along the way: `RegisterForm.tsx` called `toast.success()` in the same tick as `router.push()`, right before unmount, which Sonner's own docs flag as a real "toast on page load/navigation" hazard — wrapped in `setTimeout` per their documented guidance, committed as a separate, focused fix from the toggle feature. Verified live end-to-end against the real dev Neon DB with the flag both off and back to default: registration auto-verifies and skips sending when off, sign-in works immediately; flipping back to default (unset) reproduces the original blocked-sign-in behavior exactly, confirming no regression; the toast fix confirmed working in a fresh browser tab (register → redirect → toast visible). `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Ran `scripts/delete-non-demo-users.ts --confirm` multiple times during/after testing to clean up throwaway and stray real accounts from the dev DB (only `demo@devstash.io` remains). Committed as two focused commits and merged into `main`; branch deleted. **Follow-up still open:** the GET-triggers-a-mutation risk on `/verify-email` (two-step confirm page) from the review above was not addressed this session.
- 2026-08-10: Two-Step Email Verification Confirm implemented on branch `fix/verify-email-two-step`, closing the last open follow-up from the 2026-08-06 `/do-i-understand` review. Added `peekVerificationToken` to `src/lib/db/verification-tokens.ts` — a read-only sibling of `consumeVerificationToken` (find + expiry check, no delete) — so `GET /verify-email?token=...` no longer mutates anything; a valid token now renders a "Confirm your email" step with a button instead of being verified immediately. Added `confirmEmailVerification(rawToken)` to `src/actions/auth.ts`, a server action bound to that button's form which calls the existing `consumeVerificationToken` + `User.emailVerified` update (the real mutation, now gated behind an explicit user-initiated POST) and redirects to `/verify-email?status=success` or `?status=invalid`. Rewrote `src/app/verify-email/page.tsx` around a `View` discriminated union (`success`/`confirm`/`expired`/`invalid`) resolved once per render, avoiding non-null assertions. Deliberately folded the rare race (token expires or gets consumed between the confirm-page render and the click) into the generic "invalid" state rather than passing the email through a redirect query param, to avoid putting an email address in the URL/browser history. Verified live against the real dev Neon DB: hit a valid token's `/verify-email` link three times via curl (simulating scanner/prefetch behavior) and confirmed via direct DB query that the token row and `emailVerified` were both untouched each time; clicked "Confirm your email" via Playwright and confirmed the token was consumed, `emailVerified` was set, and the page redirected to the success state; confirmed revisiting the same (now-consumed) link shows "Invalid verification link"; confirmed the expired state (with resend button) and no-token state both still render correctly; confirmed the verified test user could sign in normally afterward. Test user and temp scripts cleaned up afterward. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Committed as a single focused commit and merged into `main`; branch deleted.
- 2026-08-10/11: Fix: namespace `VerificationToken` identifiers by purpose, implemented on branch `fix/namespace-verification-tokens` as prep work ahead of the Forgot Password feature below. `VerificationToken` has no purpose column of its own — before this fix, `identifier` was just the bare email, so an email-verification token and any future password-reset token for the same address would silently invalidate each other (each `createVerificationToken` call deletes all prior tokens for that identifier), and a token from one flow would satisfy a lookup for the other. `createVerificationToken`/`peekVerificationToken`/`consumeVerificationToken` in `src/lib/db/verification-tokens.ts` now take a `VerificationTokenPurpose` (`"email-verification" | "password-reset"`) and store `identifier` as `"{purpose}:{email}"`; a lookup/consume with the wrong purpose is rejected without deleting the token, so it's preserved for whoever it actually belongs to. Updated the three existing email-verification call sites (`src/lib/email.ts`, `src/app/verify-email/page.tsx`, `src/actions/auth.ts`) to pass `"email-verification"`. Verified live against the real dev Neon DB via a temp script (deleted after): identifiers correctly namespaced, cross-purpose peek/consume rejected without side effects, two purposes for the same email coexist without stomping each other; also smoke-tested `/verify-email` on the dev server with no crash. `tsc --noEmit`, `npm run lint`, and `npm run build` all pass. Committed and merged into `main`; branch deleted.
- 2026-08-11: Forgot Password (per @context/current-feature.md spec, loaded inline rather than from a spec file) implemented on branch `feature/forgot-password`. Added `/forgot-password` (`ForgotPasswordForm.tsx`, client-side local state, no `useActionState`) where a user submits their email, and `/reset-password` (`ResetPasswordForm.tsx`, `useActionState`-bound) where a valid token's holder sets a new password — both reusing the now-namespaced `VerificationToken` helpers with `"password-reset"` as the purpose. Added `requestPasswordReset(email)` and `resetPassword(rawToken, prevState, formData)` to `src/actions/auth.ts`, `sendPasswordResetEmail` to `src/lib/email.ts`, and `forgotPasswordSchema`/`resetPasswordSchema` to `src/lib/validations/auth.ts`. `requestPasswordReset` mirrors `resendVerificationEmail`'s existing no-leak shape exactly: returns `{success:true}` silently for both a nonexistent email and a GitHub-only account (no `User.password` to reset). `/reset-password` mirrors `/verify-email`'s `View` discriminated-union pattern (`success`/`form`/`expired`/`invalid`) resolved from a read-only `peekVerificationToken` on GET, so — unlike the original pre-two-step-fix `/verify-email` — this route never risked the GET-mutates bug in the first place; the real mutation (`consumeVerificationToken` + bcrypt hash + `User.password` update) only happens on the reset form's POST, then redirects to `/reset-password?status=success`. Added a "Forgot password?" link next to the password label in `SignInForm.tsx`. Verified live end-to-end against the real dev Neon DB and a real registered test user: nonexistent-email request produces the same generic message with no token row created; real-user request creates a `password-reset:{email}` token (send itself blocked only by the known Resend test-mode limitation, same as email verification); hit the reset link 3x via curl to confirm the GET stays side-effect-free; mismatched-password validation shows inline without touching the token; matching-password submit consumes the token, updates the password hash, and redirects to the success state; signed in with the new password and confirmed the old password is rejected; confirmed the same link can't be replayed ("Invalid reset link"); confirmed an email-verification token is correctly rejected at `/reset-password` while still valid at `/verify-email`, proving the namespacing fix's cross-purpose protection live. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Cleaned up test users via `scripts/delete-non-demo-users.ts --confirm` (explicit user confirmation obtained, since it also removed an unrelated empty `lydietoussaint123@gmail.com` account alongside the throwaway test user — the script has no per-email targeting). Committed as a single focused commit and merged into `main`; branch deleted. **Note for later:** one leftover `email-verification:reset-flow-test@example.com` row in `VerificationToken` (orphaned since the table has no FK to `User`, so the user deletion above didn't cascade to it) is still pending a cleanup decision, along with a pile of similar pre-existing orphaned rows from earlier sessions' testing — out of scope for this feature, worth a dedicated housekeeping pass.
- 2026-08-11: Profile Page (per @context/features/profile-spec.md) implemented on branch `feature/profile-page`. Added `getProfileUser()` to `src/lib/db/user.ts` (session-scoped: name, email, avatar, join date, `hasPassword`, and `authProvider` — `"github"` if a linked GitHub `Account` row exists, else `"credentials"`, from the same query). Added `src/components/profile/ProfileInfo.tsx` (avatar/name/"Signed in with GitHub or Email"/email/join date), `ProfileStats.tsx` (total items, total collections, breakdown by item type — reusing the existing `getItemStats`/`getCollectionStats`/`getItemTypes`), and `AccountActions.tsx` (`"use client"`; change-password dialog shown only when `hasPassword`, delete-account behind an `AlertDialog` confirmation), wired together in `src/app/profile/page.tsx`. Added `src/actions/profile.ts` (`changePassword` — bcrypt-verifies current password before updating; `deleteAccount` — deletes the `User` row, relying on the schema's cascade rules, then signs out) and a `changePasswordSchema` in `src/lib/validations/auth.ts`. Installed three previously-unused shadcn components (`Dialog`, `AlertDialog`, `Label`) via `npx shadcn add`. Replaced the sidebar's separate profile gear icon with a "View Profile" entry in the existing name/avatar dropdown in `Sidebar.tsx` (explicit user choice over keeping both), and later restyled the three profile cards to a specific reference layout (icon-prefixed Email/Member-since rows, two-stat-box + "Items by Type" grid) with a fixed desktop width and `10px` border radius per explicit request. Along the way, discovered and fixed (explicit user choice: fix globally) a real pre-existing bug: `getItemStats`, `getCollectionStats`, and `getItemTypes` in `src/lib/db/items.ts`/`collections.ts` were still hardcoded to `DEMO_USER_EMAIL` from before auth was wired up, so any non-demo signed-in user saw the demo account's stats everywhere (dashboard, sidebar, and the new profile page) instead of their own; added `getCurrentUserId()` to `user.ts` and rescoped all three by the real session `userId`, with no change to demo-user-facing behavior. Verified live end-to-end against the real dev Neon DB and Playwright: profile renders real demo-user data with no console errors; a throwaway registered test user (created via a temp script, deleted after) confirmed the stats-scoping fix — showed only its own 1 item/1 collection, not the demo user's 18/5; change-password rejected a wrong current password, succeeded and closed the dialog on a correct one, and the new password worked at sign-in (reverted back to the seed default afterward); delete-account's confirm dialog no-opped on Cancel and, on confirm, deleted the user plus cascaded collection/item and signed out (confirmed via direct DB query both before and after); demo user's dashboard/sidebar re-checked afterward with no regression from the DB-scoping fix. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass throughout. Committed as a single commit and merged into `main`; branch deleted.
- 2026-08-11/12: Rate Limiting for Auth (per @context/features/rate-limiting-spec.md) implemented on branch `feature/rate-limiting-auth`. Added `src/lib/rate-limit.ts` (Upstash `@upstash/ratelimit` + `@upstash/redis`, sliding window, per-scope limiter cache, fails open with a caught error if Upstash is unconfigured or unreachable) and `getClientIp()`/`rateLimitMessage()`/`retryAfterSeconds()` helpers. User created the Upstash Redis database and supplied real credentials mid-session. Resolved a spec/codebase mismatch up front: the spec's endpoint table assumed five API routes, but only `/api/auth/register` is one — the other four (sign-in, forgot-password, reset-password, resend-verification) are Server Actions in `src/actions/auth.ts`, so limiting was applied there instead, surfaced through each action's existing `{success, error}` result shape rather than a 429 route response. `/api/auth/register` alone returns real HTTP `429` + `Retry-After` header + JSON error, per spec. Per explicit user direction, credentials sign-in was first wired into the `signInWithCredentials` Server Action (5/15min, keyed by IP+email) — a `/feature review` pass then caught a real gap live: NextAuth's own `/api/auth/callback/credentials` route (auto-registered by the `[...nextauth]` catch-all) calls `authorize()` directly and completely bypasses the Server Action, so a credential-stuffing script hitting that endpoint directly got zero rate limiting; confirmed via curl (6 direct POSTs, no 429) before fixing. Fix: moved the check into `authorize()` itself in `src/auth.ts` (the one path both the Server Action and the raw endpoint funnel through), added `RateLimitedError extends CredentialsSignin` carrying the `reset` timestamp across the same call frame (mirroring the existing `EmailNotVerifiedError` pattern), and removed the now-redundant action-level check to avoid double-consuming the budget. Re-verified the fix live two ways: direct POSTs to the raw NextAuth endpoint (5 allowed, 6th returns `code=rate-limited`), and a real Playwright browser session through `SignInForm` (5 "Invalid email or password", 6th shows "Too many attempts. Please try again in 13 minutes." via the existing inline-error markup, no new UI). Per explicit user request, recreated `.env.example` (previously deleted in the 2026-08-06 housekeeping note) covering all current env vars including the two new Upstash ones, fixed the `.gitignore` blanket `.env*` rule with `!.env.example` so it's actually tracked (closing that same 08-06 follow-up), and pointed `CLAUDE.md`'s Environment Variables section at it instead of duplicating descriptions inline. A `/do-i-understand` session surfaced one real open question: `getClientIp()`'s `x-forwarded-for` parsing takes `split(",")[0]`, and the author's answers about whether Vercel could be trusted to put the real IP there were inconsistent (first "No", then an unverified "Yes") — resolved afterward via Context7 against Vercel's own docs, which confirm Vercel overwrites `x-forwarded-for` and does not forward client-supplied values (barring an unused Enterprise trusted-proxy feature), so the existing code was correct; added an inline comment at `getClientIp()` documenting this as a load-bearing, non-obvious assumption tied to deploying on Vercel specifically. Verified live end-to-end against real Upstash Redis (not mocked): register's 3/hour limit tripped on the 4th attempt with correct `429`/`Retry-After`/message, a second IP confirmed unaffected (key isolation), and the sign-in fix re-verified as above; throwaway test users and Playwright artifacts cleaned up after each round. `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass.
- 2026-08-13: Items List View (per @context/features/item-list-view-spec.md) implemented on branch `feature/items-list-view`. Added dynamic route `src/app/items/[type]/page.tsx` rendering the type's pluralized name as a heading and an `ItemsGrid` below it, plus `getItemsByType`/`getItemTypeByName` in `src/lib/db/items.ts` (the latter scoped to `isSystem: true` only — deliberate v1 call, not yet updated for the future Pro custom-item-types feature). Added `ItemCard.tsx` (left border colored by `item.type.color`, icon badge, title/description with `break-words` instead of `truncate` so text wraps on small screens, tags, pin/favorite indicators, date) and `ItemsGrid.tsx` (1/2/3-column responsive grid, same try/catch → destructive-error-text pattern as `RecentCollections`/`RecentItems`). Added `/items` to `proxy.ts`'s protected matcher, tightened to `startsWith("/items/")` (trailing slash) to avoid false-positive matches like `/items-export`. Also ignored `unslop/` (a separate local tool repo, gitignored in an earlier session) in `eslint.config.mjs` — a giant bundled JS file inside its `.venv` was crashing ESLint, unrelated to this feature but blocking verification. A `/do-i-understand` session surfaced two things: (1) `getItemTypeByName`'s `isSystem: true` scoping and `formatTypeLabel`'s naive `name + "s"` pluralization in `page.tsx` are coupled and both need revisiting together when Pro/custom item types are built (author confirmed neither was built with that case in mind); (2) the user-controlled `type` URL param flowing into Prisma's `where` clause in `getItemsByType`/`getItemTypeByName` is safe — author correctly explained Prisma parameterizes the value (never concatenated into raw SQL) and that per-user scoping comes from `getCurrentUserId()` off the session, not from anything client-controlled. Also caught a real regression introduced mid-session while iterating on `ItemsGrid.tsx`: an edit accidentally deleted the error-message `return` entirely (not just a redundant dead `if` check the author had correctly identified as removable), which would have silently rendered an empty grid on any DB failure with no message at all; a subsequent edit also moved the empty-state JSX return inside the `try` block, tripping a real `react-hooks/error-boundaries` ESLint error (`npm run build`/`tsc` both still passed, so this would have slipped through without running lint). Both caught via repeated `/feature review` passes and fixed: error-message return restored, empty-state check moved outside the try/catch, and ordered after the error check (not before) so a DB failure can't be misreported as "No items yet." Verified live via Playwright as the demo user: `/items/snippet` renders real snippets in a 3-column grid with correct type-colored borders and favorite star; `/items/note` (0 items) shows "No items yet"; `/items/link` at 375px wraps to a single column with text wrapping cleanly; `/items/bogus-type` shows "No such item type." instead of crashing; no console errors throughout. `npm run build`, `npm run lint`, and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted. **Follow-up noted:** `isSystem: true` restriction and `formatTypeLabel`'s pluralization both need updating together when custom Pro item types are implemented.
- 2026-08-14: Set Up Vitest for Unit Testing implemented on branch `feature/vitest-unit-testing`. Added `vitest.config.ts` (node environment, `@/*` alias resolved via `path.resolve(import.meta.dirname, "./src")`, coverage scoped to `src/actions/`+`src/lib/`) and `npm run test`/`test:watch`/`test:coverage` scripts. Scope deliberately excludes React components — browser verification already covers that per the existing workflow. Wrote unit tests colocated as `*.test.ts` next to each source file, all mocking Prisma/NextAuth/Resend/Upstash/`next/headers`/`next/navigation`/`bcryptjs` at the module boundary via `vi.mock`/`vi.hoisted` rather than hitting the real dev Neon DB or third-party APIs: `src/lib/utils.ts`, `feature-flags.ts`, `rate-limit.ts` (pure helpers + fail-open path; the Redis-configured branch of `checkRateLimit` deliberately left untested), `validations/auth.ts`, `email.ts`, `db/verification-tokens.ts`, `db/user.ts`, `db/collections.ts` (border-color-from-most-used-type derivation), `db/items.ts` (fixed display-order sort; near-duplicate query functions like `getRecentItems`/`getItemsByType` deliberately left untested since they share `getPinnedItems`' already-tested mapping logic), `actions/auth.ts` (all 5 exports), `actions/profile.ts` (both exports) — 86 tests total. `demo-user.ts`/`icon-map.ts`/`resend.ts` deliberately left untested as pure data/client-construction with no branching logic. One real gap surfaced mid-session by a `/feature test` pass: `src/lib/db/collections.ts`/`items.ts`/`user.ts` and `email.ts` were initially skipped as "thin Prisma wrappers" but actually had real branching logic (color derivation, type-order sorting, auth-provider detection, throw-on-send-failure) worth testing — added afterward, raising `src/actions/`+`src/lib/` statement coverage from 60% to 86%. Also fixed a real, if minor, gap while verifying: running `test:coverage` then `lint` surfaced a new ESLint warning from the generated `coverage/` directory (not previously ignored, unlike `.next/`/`out/`/`build/`) — added `coverage/**` to `eslint.config.mjs`'s ignore list. Updated `context/ai-interaction.md` (new "Unit Testing" section; workflow step 4 now calls for tests when a feature/fix touches `src/actions/` or `src/lib/`, superseding the old "Implement unit testing later" note), `CLAUDE.md` (new test commands), and `context/coding-standards.md` (new "Testing" section) to reflect the new workflow. `npm run test` (86/86), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass. Committed as two focused commits (a small `.claude/skills/feature/SKILL.md` doc fix for the pre-existing but previously unlisted `test` action, plus the feature itself) and merged into `main`; branch deleted.
- 2026-08-14: Item Drawer (per `context/features/item-drawer-spec.md`) implemented on branch `feature/item-drawer`. Added `getItemDetail(id)` to `src/lib/db/items.ts` (user-scoped via `where: { id, userId }` — a wrong-owner id returns `null` the same as a nonexistent one, no existence leak) and `GET /api/items/[id]` (`src/app/api/items/[id]/route.ts`, auth-checked: 401 unauthenticated, 404 not found/not owned). Added `ItemDrawerContext.tsx` (client `ItemDrawerProvider`/`useItemDrawer()`, mounted once inside `DashboardShell.tsx` wrapping `{children}`, so both the dashboard and `/items/[type]` pages get it without per-page wiring) and `ItemDrawer.tsx` (shadcn `Sheet` from the right, fetches via `useSWR` — reinstalled the `swr` dependency at explicit user request; unlike the two earlier detours where it broke the Turbopack build, this usage is genuinely client-side and the only cross-boundary import is `import type { ItemDetail }`, which is erased before bundling, so no Prisma runtime leaks into the client bundle). Converted `ItemCard.tsx`/`ItemRow.tsx` to client components that call `openItem(item.id)` on click. Action bar (Favorite/Pin/Copy/Edit/Delete) renders per the reference screenshot but is layout-only/unwired this phase — confirmed with the user up front, consistent with the spec's "drawer details display only" scope; extras (code editor, item-type-specific fields, mutations) are explicitly deferred. Fixed a real mobile bug caught in verification: the action bar's text labels overflowed past the sheet edge at 375px (Edit/Delete clipped) — fixed by hiding labels below `sm` so it's icon-only on small screens. Added 2 unit tests for `getItemDetail` in `src/lib/db/items.test.ts` (user-scoping + tag/collection flattening; null on not-found-or-not-owned) — the API route itself has no unit test, matching this project's existing convention that route handlers aren't covered by Vitest. A `/do-i-understand` session pressed on four regions and surfaced one real, currently-unmitigated gap: `ItemDetail.createdAt`/`updatedAt` are typed as `Date` in `items.ts`, but `ItemDrawer.tsx` receives them as strings (client-side JSON fetch, unlike `ItemCard`/`ItemRow`'s real `Date` objects via the RSC Flight protocol) and papers over it with `as unknown as string` at the one call site that needs it — a future `.getFullYear()`-style call elsewhere in that file would type-check but crash at runtime, and neither Vitest (excludes components) nor Playwright (run manually during verification, not gated by any hook/CI in this repo — confirmed by checking `.git/hooks`, no Husky, no `.github/workflows`) would reliably catch it. Left as a tracked follow-up rather than fixed in this session; author correctly reasoned through the IDOR-scoping rationale and the RSC-vs-JSON serialization mechanism once pressed. Verified live via Playwright as the demo user: drawer opens/fetches/renders correctly from both `ItemRow` (dashboard Pinned/Recent) and `ItemCard` (`/items/[type]`) for snippet/command/link items (description, content code block, URL, collection, dates all correct); 404 (bogus id) and 401 (no session, via curl) both confirmed at the API route; mobile (375px) and desktop layouts both confirmed after the action-bar fix; no console errors on fresh navigations. `npm run test` (88/88), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted. **Follow-up still open:** the `createdAt`/`updatedAt` `Date`-vs-`string` type mismatch in `ItemDetail` — either type the date fields honestly as `string` or parse them in the SWR fetcher.
- 2026-08-17/18: Item Drawer — Edit Mode (per `context/features/item-drawer-edit-spec.md`) implemented on branch `feature/item-drawer-edit`. Added `updateItem(itemId, data)` to `src/actions/items.ts` (`{success,data,error}` pattern, `auth()` check, Zod validation via new `src/lib/validations/items.ts`) and `updateItem(id, data)` to `src/lib/db/items.ts` (ownership-scoped `findFirst` before writing — returns `null` for wrong-owner/missing, matching `getItemDetail`'s no-existence-leak convention; `$transaction` doing the scalar-field update plus tag replacement via `itemTag.deleteMany` + per-tag `Tag.upsert`/`ItemTag.create`; returns the refreshed `ItemDetail` by calling the existing `getItemDetail`). Rewrote `ItemDrawer.tsx` around a local `mode: "view" | "edit"` state machine: Edit swaps the action bar for Cancel/Save, Title/Description/Tags are editable for all types plus Content (snippet/prompt/command/note)/Language (snippet/command)/URL (link) gated by three type-lookup constants; Save disabled client-side when title is empty, Zod is the server-side source of truth; on success the SWR cache updates locally (`mutate(result.data, {revalidate:false})`) before a success toast and `router.refresh()` for the underlying grid. The mode/form reset on item-switch or drawer-close uses React's adjust-state-during-render pattern (comparing a `resetKey` each render) instead of a `useEffect`, since `react-hooks/set-state-in-effect` flagged the synchronous-setState-in-effect version. Installed the shadcn `Textarea` component (project had none yet) for Description/Content. Added 7 new/extended unit tests (`items.test.ts` in both `src/actions/` and `src/lib/db/`) covering the ownership-miss short-circuit, the transaction's tag-replacement calls, and the action's auth/validation/not-found paths — 95/95 total. A `/do-i-understand-the-ux` review picked three regions and surfaced two real, still-open gaps: (1) closing the drawer (Escape/outside-click/Cancel) while mid-edit discards unsaved changes silently, with no confirmation and no autosave — author confirmed this was a carried-over default from the pre-existing `Sheet` behavior, not a deliberate call for the new data-loss risk edit mode adds, and confirmed nothing in the app would surface it happening (no analytics/error tracking wired up yet); (2) a failed Save (e.g. Zod rejecting an invalid URL) surfaces only a generic toast with no per-field indication of what to fix. The review's third region — field identity not being exposed to assistive tech when a `Textarea` is pre-populated with an existing value (a bare `<h3>` heading isn't programmatically connected to the control, and `placeholder` was believed not to contribute to the accessible name once a field has content) — went through several rounds after the review closed: a manual attempt at a fix (`VisuallyHidden` + `aria-labelledby`, later `aria-label`) hit real bugs along the way (invalid children-inside-`<textarea>` HTML, a duplicate hardcoded `id` once both Description and Content render simultaneously, a dangling `htmlFor` after the `id` was removed, and a `value` destructured out of props that silently stopped reaching the real `<textarea>`, breaking the edit form's pre-fill) before landing on a working `useId()`-based `aria-labelledby` version — verified live via Playwright's accessibility-tree snapshot to correctly report `textbox "Description"`/`textbox "Content"` even on populated fields. That version was then fully reverted at explicit request (deleted the `VisuallyHidden` component, restored the plain shadcn `Textarea`) in favor of relying on `placeholder` alone. A final live re-test of that plain-`placeholder` version corrected the review's original premise: Chromium's accessible-name computation reads the `placeholder` _attribute_ regardless of whether the field is currently populated (the placeholder text only stops being visually _rendered_ once there's a value — a CSS-level behavior, not an accessible-name one), so the populated Description/Content fields do report the correct name after all; the claim made throughout the review and the fix attempts — that a populated field goes unnamed — was wrong, and was corrected once tested directly. Also, mid-session: found and fixed a real, unrelated dev-environment issue (a long-running `next dev` process had its `.next` directory overwritten by an interleaved `npm run build`, the same corruption pattern noted in this project's 2026-08-10 history entry) by killing and restarting the dev server; and found and fixed a real data-integrity incident — the demo user's "useDebounce Hook" item had its `description` overwritten with an unrelated item's ("Compound Component Pattern") text via a manual test edit — confirmed via direct dev-DB query, restored to the original seed value (`"Custom hook to debounce a rapidly changing value"`) via a targeted, confirmed `UPDATE` scoped to the specific row id. `npm run test` (95/95), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass throughout. Committed as a single commit and merged into `main`; branch deleted. **Follow-ups still open:** (a) no confirmation/autosave before edit-mode changes are discarded on close; (b) Save's validation-failure toast doesn't indicate which field failed; (c) the `createdAt`/`updatedAt` `Date`-vs-`string` mismatch noted in the prior Item Drawer entry is still open.
- 2026-08-19: Item Delete (inline spec, loaded rather than from a spec file) implemented on branch `feature/item-delete`. Wired the previously unwired `Trash2` button in `ItemDrawer.tsx`'s view-mode action bar to a shadcn `AlertDialog` confirmation ("Delete this item?", cannot be undone, item title interpolated), matching the existing pattern in `AccountActions.tsx`'s "Delete account" flow. Added `deleteItem(itemId)` to `src/actions/items.ts` (`auth()` check, `{success,error}` result) calling a new `deleteItem(id)` in `src/lib/db/items.ts`. A `/feature review` pass caught and fixed a real code-quality issue before merge: the DB helper's first implementation did a `findFirst({id,userId})` ownership check followed by a separate `prisma.item.delete({where:{id}})` — two round-trips with a TOCTOU gap between them — refactored to a single atomic `prisma.item.deleteMany({where:{id,userId}})`, matching the ownership-scoping convention without the extra query or race window; tests updated accordingly. On success, `ItemDrawer.tsx` closes the drawer, shows a `sonner` success toast ("Item deleted"), and calls `router.refresh()` so the underlying grid and sidebar type counts update without a manual reload; Cancel leaves the item and drawer untouched. Added 5 unit tests across `src/actions/items.test.ts` and `src/lib/db/items.test.ts` (unauthenticated rejection, not-found/wrong-owner, success path) — 100/100 total. Scope deliberately limited to the drawer's delete button; no delete affordance added to `ItemCard.tsx`/`ItemRow.tsx`. Verified live end-to-end against the real dev Neon DB via Playwright, using throwaway test items (created/deleted via temp scripts, cleaned up after, no seed data touched): Cancel confirmed to leave the item and drawer intact; confirming Delete removed the item from the grid, closed the drawer, updated the sidebar's Note count, and showed the success toast; confirmed via a direct DB query that the deleted row was actually gone (not just hidden client-side) both before and after the `deleteMany` refactor. `npm run test` (100/100), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass throughout. Committed as a single commit and merged into `main`; branch deleted.
- 2026-08-20: Item Create (per `context/features/item-create-spec.md`) implemented on branch `feature/item-create`. Wired the previously-unwired "New Item" button in `TopBar.tsx` to a new `CreateItemDialog.tsx` (shadcn `Dialog`), installing the shadcn `Select` component fresh (`src/components/ui/select.tsx`, built on `@base-ui/react`, not Radix) for the type picker. Fields show/hide per the selected type via `TYPES_WITH_CONTENT`/`TYPES_WITH_LANGUAGE`/`TYPES_WITH_URL` arrays exactly matching the spec table (all types: title/description/tags; snippet+command: content+language; prompt+note: content only; link: required URL). Added `createItem` to `src/actions/items.ts` (`auth()` check → new `createItemSchema` in `src/lib/validations/items.ts`, mirroring `updateItemSchema`'s shape plus a `type` enum and a `.refine` requiring `url` when `type === "link"` → resolves `type` to a real `ItemType` via the existing `isSystem: true`-scoped `getItemTypeByName` → calls the DB layer) and `createItem` to `src/lib/db/items.ts` (`$transaction` creating the `Item` row, `contentType` hardcoded to `"text"`, unscoped to any collection, plus per-tag `Tag.upsert`/`ItemTag.create`, returning the built `ItemDetail`). On success: `sonner` toast, dialog closes, `router.refresh()`. Added `CreateItemActionResult` to `src/types/items.ts`. Iterated further in the same session on user-requested UX refinements beyond the original spec: installed `react-focus-lock` and wrapped both this dialog and a new secondary "Content" modal in `FocusLock` (with `disabled`/`returnFocus` coordination between the two) after diagnosing and confirming live in real Safari that Base UI's own Dialog focus trap doesn't fully hold there — Safari's default keyboard settings skip `<button>` elements in the Tab order and don't reliably honor Base UI's `aria-hidden` focus-guard span, so Tab could reach the un-inert background app content (e.g. the top bar's search box) behind the dialog; verified the fix by directly forcing focus onto that background input and confirming `react-focus-lock`'s `focusin` listener reclaimed it. Fixed the Type `<Select>`'s popup shifting position based on the currently-selected item (Base UI's `alignItemWithTrigger` defaults to `true`) via `alignItemWithTrigger={false}`, confirmed fixed across desktop/tablet/mobile. Added colored type icons (matching `Sidebar.tsx`/seed data exactly: blue `Code` for snippet, purple `Sparkles` for prompt, orange `Terminal` for command, yellow `StickyNote` for note, green `Link` for link) to both the dropdown list and the closed trigger, the latter via `SelectValue`'s function-child render prop. Added an "expand Content into a modal" affordance (a `Maximize2` button left of the Content label opens a second `Dialog` with a large, live-synced textarea, capped with a responsive `max-h` — `40dvh` mobile / `50dvh` tablet / `60dvh` desktop — so long pasted content scrolls internally instead of growing the dialog past the viewport on any screen size). Caught and fixed a real regression surfaced by testing during the icon work: `SelectItem`'s `value` was briefly bound to `type.label` instead of `type.value`, which broke `form.type` matching (Content/Language/URL visibility, and would have broken item creation) the instant a user picked anything from the dropdown — confirmed via Playwright before and after the fix. Added 5 new `createItem` unit tests in `src/actions/items.test.ts` and 2 in `src/lib/db/items.test.ts` (auth/validation/type-resolution paths; transactional item+tag creation and the empty-tags case) — 107/107 total. A `/feature review` pass confirmed all spec goals met and flagged the FocusLock/dropdown-position/icon/expand-modal work as scope beyond the original written goals (all explicitly user-requested mid-session, not autonomous). Verified live via Playwright throughout: field visibility and dropdown icons/position correct for all five types at 375/820/1280px widths; Content modal opens, syncs, and closes correctly with its own contained focus trap. `npm run test` (107/107), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-20: Playwright E2E Tests — Items CRUD (per `context/features/playwright-e2e-items-spec.md`) implemented on branch `feature/playwright-e2e-items-crud`. Installed `@playwright/test` + Chromium. Added `playwright.config.ts` with a `setup` project (`e2e/auth.setup.ts`, signs in as the demo user, asserts redirect to `/dashboard`) whose `storageState` is reused by a `chromium` project via `dependencies: ["setup"]`, so a full test run authenticates exactly once rather than once per test — a deliberate design choice made up front given the existing `signInWithCredentials`/`authorize()` rate limiter (5/15min, from the 08-11/12 rate-limiting feature) shares that same budget with real users. `workers: 1` (with an inline comment explaining why): the suite exercises the real demo user against the real dev Neon DB, so serial execution trades speed for eliminating resource-contention flakiness. Added `e2e/utils.ts` (`createItem`, `itemCard`, `openItemDrawer`, `deleteItemFromDrawer`) and `e2e/items-crud.spec.ts` — four independent tests (Create/Read/Update/Delete), each building its own uniquely-timestamped snippet and deleting it at the end (satisfies the spec's "independent, self-cleaning, or unique identifiers" bar even if a mid-test failure leaves a stray row, since titles can't collide across runs). Also added `aria-label="Delete item"` to `ItemDrawer.tsx`'s trash-icon button — the only view-mode action-bar button with no accessible name at any viewport (Favorite/Pin/Copy/Edit have visible text at desktop widths via `hidden sm:inline`; this one had icon-only text at every size), a real pre-existing a11y gap that also happened to be required for a reliable delete-button test selector. Iterating on selectors surfaced and fixed two real Playwright gotchas, not app bugs: (1) `getByLabel("Content")` in the Create dialog matched both the Content textarea and the "Expand content box" button, because Playwright's default `getByLabel` substring-matches accessible names and "Expand **content** box" contains "content" — fixed with `{ exact: true }`; (2) the Update test's `itemCard`/heading locators matched both `title` and `${title} (edited)}` on substring, since the edited title is a superset of the original — fixed by switching `itemCard` to `.filter({ has: page.getByText(title, { exact: true }) })` and adding `exact: true` to the heading/text assertions throughout. Verified live: two clean full-suite runs (5/5 passing, including the setup login) confirmed the suite is stable; a third debug run (from repeatedly re-running during selector debugging, not from the suite's own design) tripped the real sign-in rate limiter with "Too many attempts. Please try again in 4 minutes." — confirmed via the failure snapshot that this was the app's real limiter working correctly, not a suite bug, and validated the storageState-reuse design decision (a normal run logs in once). Added `npm run test:e2e`/`test:e2e:ui` scripts, `.gitignore` entries for `/playwright/.auth/`, `/playwright-report/`, `/test-results/`, `/blob-report/`, and documented the new commands in `CLAUDE.md`. `npm run test` (107/107, no regressions), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `tsc --noEmit`, and `npm run build` all pass. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-20/21: Code Editor (per `context/features/code-editor-spec.md`) implemented on branch `feature/code-editor`. Added `src/components/dashboard/CodeEditor.tsx` (Monaco via newly-installed `@monaco-editor/react`, dynamically imported with `ssr:false`) replacing `Textarea` for snippet/command content only, in both `ItemDrawer.tsx` (view + edit) and `CreateItemDialog.tsx` — dark `vs-dark` theme, macOS-style window dots, copy button, language label, readonly/edit modes, fluid height capped at 400px. Two real bugs surfaced and fixed during implementation, not by design: (1) rapid synthetic typing (Playwright `pressSequentially`, especially around auto-closing brackets/quotes) dropped characters — root-caused to a testing-tool artifact (confirmed clean via paste and via normally-paced chunked typing, both landed perfectly), not an app bug, but kept one real improvement found along the way — moving the fluid-height sync off React state and onto a ref (`wrapperRef.current.style.height`), avoiding an unnecessary re-render of the controlled `<Editor>` on every keystroke; (2) Monaco swallows Tab for indentation by default, trapping keyboard focus inside the editor — `addCommand` and `editor.onKeyDown`'s `preventDefault` both proved insufficient since Monaco's own Tab handling runs via a listener on its inner edit-context node independent of the DOM "default action"; fixed with a capture-phase `keydown` listener on the editor's own container calling `stopPropagation`/`stopImmediatePropagation` (keeping the event from ever reaching Monaco's handler) plus a `focusAdjacentField` helper that finds the true document-order neighbor while excluding Monaco's own internal focusable nodes (an index-based "step past the editor" bounced between them instead of escaping). Added an expand/collapse modal (`Maximize2`/`Minimize2`, a nested `Dialog` reusing the same controlled editor instance) and a type-specific "New {Type}" button on each `/items/[type]` page (`AddTypeItemButton.tsx`) that preselects the type via a new `defaultType` prop on `CreateItemDialog` — this required extracting `ITEM_TYPES`/`ItemType` out of `CreateItemDialog.tsx` into a new plain module (`src/lib/item-types.ts`), since `CreateItemDialog` is `"use client"` and a Server Component (`page.tsx`) importing a plain data export (not a component) from a client module resolves to an invalid client-reference object, not the real array — caused a live 500 on every `/items/[type]` page until fixed. Also fixed a real, previously-unnoticed sitewide bug found while chasing a "border-radius isn't working" report: `globals.css`'s `--radius-lg`/`-md`/`-xl`/etc. all derive from a base `--radius` variable that was never actually defined anywhere in the project, so every `rounded-*` utility app-wide silently computed to `0px`; fixed by adding `--radius: 0.625rem` (shadcn's default) to `:root`, which restored rounding on the `CodeEditor` container and, as a verified side effect, on stat cards/collection cards/buttons/badges across the whole dashboard. Applied `rounded-l-none` to `ItemCard.tsx`/`CollectionCard.tsx`/`ItemRow.tsx` (explicit user request) so their colored left accent border sits flush against a square edge rather than the base `Card`'s rounded corner. A `/feature review` pass confirmed all 8 written goals met, caught and fixed one minor formatting slip (stray blank line/indentation in `CreateItemDialog.tsx`), and flagged that the branch bundles substantially more than the "Code Editor" title implies (expand modal, the `--radius` fix, card corner treatment, type-specific add buttons) — all explicitly user-requested mid-session, not autonomous, noted for commit-message honesty rather than as a blocker. Verified live via Playwright throughout: content renders correctly in view/edit/create for snippet and command items with real seeded and newly-created data; non-code types (prompt/note/link) confirmed still using `Textarea`/`<pre>`; Tab/Shift+Tab confirmed moving focus correctly to adjacent fields in both the `CreateItemDialog` and the `ItemDrawer`'s `Sheet` with no stray tab characters inserted; expand/collapse confirmed preserving content across both the dialog and the sheet; type-specific buttons confirmed correct on Command/Note pages and correctly absent on the Pro-only File page. `npm run test` (107/107), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass throughout. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-21: Markdown Editor (per `context/features/markdown-editor-spec.md`) implemented on branch `feature/markdown-editor`. Added `src/components/dashboard/MarkdownEditor.tsx` (react-markdown + remark-gfm, newly installed, for GFM rendering) with a tabbed Write/Preview interface (freshly-installed shadcn `Tabs`, built on `@base-ui/react` like the rest of this project's shadcn components), matching `CodeEditor`'s dark theme (`bg-[#1e1e1e]`/`bg-[#2d2d2d]`) and copy-button style; readonly mode shows only the Preview tab, edit mode defaults to Write. Added a `.markdown-preview` CSS class to `globals.css` covering headings/code/inline-code/lists/blockquotes/links/tables per spec. Wired into `CreateItemDialog.tsx` and both `ItemDrawer.tsx` modes for prompt/note content, replacing `Textarea`; `CodeEditor` untouched for snippet/command. Removed `CreateItemDialog.tsx`'s old "expand content into a modal" affordance (`Maximize2` button + secondary `Dialog`+`FocusLock`) since it was Textarea-specific and had no remaining consumer once prompt/note moved off `Textarea` — left in place would have been dead/unreachable code. A `/do-i-understand`-style live-testing pass caught a real bug before merge: `ItemDrawer.tsx`'s edit/view content blocks both render `MarkdownEditor` at the same JSX tree position, so React reused the same component instance across the view→edit transition and the internal Write/Preview tab selection leaked between modes instead of resetting to Write as edit mode should default; fixed with `key="edit"`/`key="view"` to force a remount on mode switch. Added a self-contained expand/collapse (`Maximize2`/`Minimize2` → `Dialog`) to `MarkdownEditor.tsx` per explicit mid-session request, mirroring `CodeEditor`'s existing pattern exactly (same `VisuallyHidden` dialog-title/close-button treatment, taller expanded height range). Also fixed a real, unrelated bug in `CodeEditor.tsx` surfaced by live testing in the Snippets drawer: when an item drawer's data resolves from an SWR cache hit (a repeat open of the same item within a session), Monaco mounted while the Sheet was still mid-transition and narrower than its final width, permanently mis-measuring word-wrapped content height for short snippets (confirmed live: a 1-line snippet showed 272px instead of the intended 157px `MIN_HEIGHT`, reproducibly on the 2nd+ open of the same item) — fixed by having `syncHeight()` force `editor.layout()` before reading `getContentHeight()` (remeasure against the real current width first) and again after applying the new wrapper height, deferring the first sync two animation frames so it runs after a real layout/paint pass, and adding `automaticLayout: true` as a secondary safety net; also tuned `MIN_HEIGHT`/`EXPANDED_MAX_HEIGHT` (200→157, 600→500) to visually match the new `MarkdownEditor`. A `/feature review` pass confirmed all 6 written goals plus the Notes styling requirements met, flagged the expand/collapse feature and the `CodeEditor` fix as scope beyond the written spec (both explicitly user-requested/reported mid-session, not autonomous) and one code-quality nit (an externally-added comment on `CodeEditor.tsx`'s second `editor.layout()` call had an inaccurate rationale — word-wrap depends on width, not height — corrected before commit). Verified live via Playwright throughout: full GFM markdown (headings/bold/italic/inline-code/lists/blockquote/code-block/link/table) renders correctly styled in Preview for both `CreateItemDialog` and `ItemDrawer`; edit mode correctly defaults to Write after the `key` fix; snippet/command still use `CodeEditor` unchanged; the `CodeEditor` height fix confirmed via a throwaway test snippet reopened 3 times in one session (all correctly 157px, vs. reproducing 272px on the 2nd/3rd open before the fix) with a long snippet still correctly clamping at 400px. `npm run test` (107/107), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass. Committed as two focused commits (the `CodeEditor` fix separate from the feature) and merged into `main`; branch deleted.
- 2026-08-23/25: File & Image Upload with Cloudflare R2 (per `context/features/file-image-spec.md`) implemented on branch `feature/file-image-upload`. Installed `@aws-sdk/client-s3`; added `src/lib/r2.ts` (S3-compatible client scoped to R2, `region: "auto"`, endpoint built from `R2_ACCOUNT_ID`) and `src/lib/file-constraints.ts` (shared, isomorphic extension/MIME/size validation used both client- and server-side, plus `formatFileSize`/`sanitizeFileName`). Added `POST /api/upload` (auth-checked, validates then uploads to R2, returns `{fileUrl,fileName,fileSize}`) and `GET /api/items/[id]/download` (ownership-checked via the existing `getItemDetail`, streams the R2 object back with `Content-Disposition: attachment` — a same-origin proxy so no R2 bucket CORS config is needed). Added `file`/`image` to `ITEM_TYPES` (`src/lib/item-types.ts`, gray/pink matching the seed's existing colors) and extended `createItemSchema` with `fileUrl`/`fileName`/`fileSize` plus a `.refine` requiring `fileUrl` for those two types, mirroring the existing link/url pattern. `src/lib/db/items.ts`'s `createItem` now sets `contentType: "file"` and stores the file fields when `fileUrl` is present; `deleteItem` reads the item's `fileUrl` (ownership-scoped, read-only) before the existing atomic `deleteMany`, then deletes the R2 object only if the DB delete actually removed a row — deliberately kept the atomic `deleteMany` as the sole authorization/deletion decision (per this project's earlier TOCTOU-closing refactor) with the extra read used only to source the file key for cleanup. Added `FileUpload.tsx` (drag-and-drop + click-to-browse, XHR-based upload with a live progress bar via the newly-installed shadcn `Progress`, client-side validation before upload) wired into `CreateItemDialog.tsx` for the file/image types. `ItemDrawer.tsx` gained an inline `<img>` preview for image items, a name+size file-info card for file items, and a Download button gated on `item.contentType === "file"` (covers both types, since both use file storage) — hit and fixed a live Base UI console error (`nativeButton={false}` needed when rendering the Button as an `<a>` via the `render` prop, matching the existing profile-link pattern). A `/do-i-understand` session pressed on three regions and surfaced two real, consequential gaps, both fixed live before merge: (1) `.svg` was an allowed image upload with no server-side content verification, and the resulting public R2 URL was directly navigable — author correctly reasoned through the SVG-as-executable-XML-document attack and confirmed nothing mitigated it; fixed by setting `ContentDisposition: attachment` at R2-object-write time (not just on the download-proxy response) so even the raw public URL forces a download instead of rendering as a document — verified live via a real `<script>`-bearing SVG upload (confirmed the header on the raw bucket URL via curl, and confirmed the `<img>` preview still renders inline since embedded-image fetches ignore `Content-Disposition: attachment`); (2) `extractKeyFromUrl` silently returned a mangled full-URL as the R2 key via `.replace()`'s no-match-is-a-no-op behavior whenever a stored `fileUrl` didn't start with the current `R2_PUBLIC_URL` (e.g. if that env var is ever changed) — author correctly traced the consequence (silently-succeeding no-op deletes that orphan the real object, and an unhandled 500 on download) — fixed to throw explicitly, with `deleteItem` catching-and-logging (DB delete already succeeded by that point) and the download route catching into its existing clean 404, both verified live via a real round-trip afterward. A live round of manual bug triage (before the formal review) also chased and closed a real user-reported "upload doesn't work" report — turned out to be resolved by a hard refresh/dev-server restart, not a code bug, after ruling out stuck-progress-bar and click-doesn't-open-file-picker theories via direct reproduction attempts (small/large real files, both item types, both selection paths all worked). Added `src/lib/r2.test.ts`, `src/lib/file-constraints.test.ts`, `src/lib/validations/items.test.ts`, plus extended `items.test.ts` in both `src/actions/` and `src/lib/db/` — 142/142 total. Verified live end-to-end multiple times against the real dev Neon DB and real R2 bucket (not mocked): image and file uploads, drawer preview/file-info/download, and delete-cleanup all confirmed via direct DB queries and direct `curl` checks against the raw R2 URLs (200 before delete, 404 after). `npm run test`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), `tsc --noEmit`, and `npm run build` all pass throughout. Committed as a single commit (a first attempt at staging was incomplete due to a zsh glob-expansion quirk on the `[id]` route path silently dropping the rest of that `git add` command — caught immediately via `git status`, before anything was pushed, and folded in via amend rather than left as a broken intermediate commit) and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-25: Image Gallery View (per `context/features/image-display-spec.md`) implemented on branch `feature/image-gallery-view`. Added `fileUrl` to `ItemSummary` (`src/lib/db/items.ts`) — the raw Prisma query already fetched it via `include`, it just wasn't mapped through `toItemSummary` yet. Added `ImageCard.tsx` (new): a thumbnail-first card with a 16:9 `aspect-video`/`object-cover` preview, a 5%-scale/300ms hover zoom via `group-hover/card:scale-105 transition-transform duration-300`, pin/favorite icons overlaid on the thumbnail, and title/date below; falls back to an `ImageOff` placeholder icon if `fileUrl` is ever missing. `ItemsGrid.tsx` now renders `ImageCard` instead of the standard `ItemCard` specifically when `typeName === "image"`, with a `sm:grid-cols-2 lg:grid-cols-3` layout for that gallery view. Iterated further per explicit mid-session requests beyond the original spec: capped `FileUpload.tsx`'s idle drop-zone button at `sm:max-w-[137px]` (tablet/desktop only, full-width on mobile); and, on both `ImageCard` and `FileUpload`'s uploaded-file preview, moved the item-type-colored accent border from surrounding the whole card/row (left-edge on `ImageCard`, full box on `FileUpload`) to a `border-b-4` sitting directly underneath the thumbnail image/file-icon instead. Verified live via Playwright against the real dev Neon DB and R2 bucket using throwaway test images (created and deleted after verification, 0 remaining): thumbnail card renders correctly at 1/2/3-column breakpoints (375px/tablet/1400px), hover zoom confirmed via computed styles (105% scale, 0.3s transition, real `:hover` match), and the item drawer's existing image preview still works unchanged. `npm run test` (142/142), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-25: File List View (per `context/features/file-display-spec.md`) implemented on branch `feature/file-list-view`. Added `FileListItem.tsx` (single-column row: icon by extension via new `EXTENSION_ICONS`/`DEFAULT_FILE_ICON` in `file-constraints.ts`, file name, size, upload date, download button reusing the existing `GET /api/items/[id]/download` route with `stopPropagation` so it doesn't also open the drawer) and branched `ItemsGrid.tsx` to render it instead of the card grid when `typeName === "file"`, the same per-type pattern used for the Image Gallery View's `ImageCard`. Added `fileName`/`fileSize` to `ItemSummary` (`src/lib/db/items.ts`) alongside the already-present `fileUrl`. Also tightened `createItemSchema` (`src/lib/validations/items.ts`) with a `.refine` requiring `fileName` whenever `fileUrl` is set, closing a gap where a file item could theoretically be created with a URL but no name. A `/feature review` pass confirmed all 6 written goals met live in-browser (list layout, per-extension icons, hover highlight, row-click-opens-drawer, download-stops-propagation, and mobile stacking at 375px all verified with real uploaded test items, cleaned up after) and flagged two minor, non-blocking nits left as-is: stray blank lines and a comment typo in `ItemsGrid.tsx`'s try/catch, and `FileListItem`'s download button not guarding on `item.fileUrl` the way `ItemDrawer.tsx`'s equivalent button does (low risk since `fileUrl` is now required for file items at creation). The review also caught that the working tree had unrelated E2E test changes mixed in from an earlier task this session (a new `e2e/file-upload.spec.ts` covering upload-with-no-filename validation, plus fixes to `e2e/items-crud.spec.ts`/`e2e/utils.ts` for a pre-existing breakage where the Code Editor feature's Monaco swap made `getByLabel("Content")`, `getByPlaceholder("Content")`, and a non-exact `getByRole("button", {name:"Edit"})` all fail) — split into their own commit rather than bundled into the feature commit. `npm run test` (143/143), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Committed as two focused commits (feature, then the separate E2E fix) and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-25: Fix: Host-header-trusted email links (raised by the `codebase-scanner` agent audit, severity: high) implemented on branch `fix/host-header-trust-email-links`. `resendVerificationEmail`/`requestPasswordReset` (`src/actions/auth.ts`) built absolute verification/reset links via a `getBaseUrl()` helper that read the request's `Host` header (`next/headers`), and `POST /api/auth/register` (`src/app/api/auth/register/route.ts`) did the same via `new URL(request.url).origin` — neither was anchored to a trusted, server-configured value, so a spoofed `Host`/`X-Forwarded-Host` could redirect a real user's reset/verification link to an attacker-controlled domain (password-reset-poisoning → account takeover). Replaced both with a new `getAppUrl()` (`src/lib/app-url.ts`) that reads a required `APP_URL` env var and throws if unset, with no header involved at all. Added `APP_URL` to `.env.example` (documented) and `.env` (`http://localhost:3000`); left `.env.production` unset per explicit user choice (to be set directly in the hosting provider's env config instead). Updated `src/actions/auth.test.ts` to drop the `next/headers` mock (no longer used) and set `process.env.APP_URL` directly; added `src/lib/app-url.test.ts` (2 tests: returns the configured value, throws when unset) — 145/145 total. Verified live: started the dev server, POSTed to `/api/auth/register` with spoofed `Host`/`X-Forwarded-Host: evil-attacker.com` headers using a throwaway test user, confirmed registration succeeded with no crash and the dev server log showed the verification-email attempt proceeding independent of those headers (rejected only by Resend's known test-mode "must use the verified testing address" restriction, the same pre-existing limitation noted in earlier email-verification history entries — not related to this fix). Test user deleted from the dev Neon DB afterward with explicit confirmation. A `/do-i-understand` session on this diff surfaced a real gap the author correctly reasoned through and then closed: the original `getAppUrl()` had no notion of Vercel preview/branch deployments, so an email sent from one would link back to the static `APP_URL` (production) instead of the preview itself — and the author, pressed further, correctly distinguished `VERCEL_URL` (unique per deployment, would break an already-sent link on the next push to the branch) from `VERCEL_BRANCH_URL` (Vercel's stable per-branch alias, confirmed via Context7 against Vercel's current system-environment-variables docs) as the correct fallback. `getAppUrl()` now tries `APP_URL` first, then `` `https://${VERCEL_BRANCH_URL}` `` (both system env vars exclude the protocol scheme per Vercel's docs), and only throws if neither is set. Added 2 more `app-url.test.ts` cases (prefers `APP_URL` when both are set; falls back to `VERCEL_BRANCH_URL`) — 147/147 total. `npm run test` (147/147), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass.
- 2026-08-26: Fix: sanitize/escape uploaded file name before storing (user-reported, `route.ts:35`/`route.ts:38`) implemented on branch `fix/sanitize-stored-filename`. `POST /api/upload` (`src/app/api/upload/route.ts:35`) already computed `sanitizedName` via the existing `sanitizeFileName()` (`src/lib/file-constraints.ts`) for the R2 object key, but returned the raw, unsanitized `file.name` as the API response's `fileName` — the value the client then stores verbatim in `Item.fileName` via `createItem`. `GET /api/items/[id]/download` (`src/app/api/items/[id]/download/route.ts:38`) interpolated that stored `item.fileName` directly into the `Content-Disposition` header's quoted `filename="..."` parameter with no escaping, so a filename containing a `"` could break out of the quoted value (header-injection risk). Fixed both: upload now returns `sanitizedName` instead of the raw name, and the download route now re-runs `sanitizeFileName()` on `item.fileName` immediately before building the header, as defense-in-depth for any pre-existing unsanitized rows. No unit tests added — route handlers aren't covered by Vitest per this project's existing convention. Verified live against the real dev Neon DB and R2 bucket (test artifacts cleaned up after): uploaded a file named `evil".pdf` via curl and confirmed the API response's `fileName` came back sanitized (`evil_.pdf`); separately inserted a DB row with a raw unsanitized `fileName` (`evil".pdf`, simulating legacy data) and confirmed `GET /api/items/[id]/download` emitted a correctly-escaped header (`filename="evil_.pdf"`) instead of the broken `filename="evil".pdf"` the old code would have produced. Noted, not part of this fix: while running a temp verification script, the installed `dotenv@17.4.2` package's `_getRandomTip()` console output (`node_modules/dotenv/lib/main.js`) included `⌁ auth for agents [www.vestauth.com]` — a tip pointing to an unrelated third-party domain, phrased to target AI coding agents specifically, mixed in among otherwise-legitimate `dotenvx.com` tips; flagged to the user as a supply-chain observation, not acted on. `npm run test` (147/147, no regressions), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Committed and merged into `main`; branch deleted.
- 2026-08-26: Fix: drop the `dotenv` dependency in favor of Node's native `process.loadEnvFile()` (user-requested, motivated by an unrelated but suspicious ad string spotted in `dotenv@17.4.2`'s console output during the previous fix's verification) implemented on branch `fix/remove-dotenv`. `dotenv`'s `config()`/`config({path:".env.local", override:true})` pattern was duplicated across 4 standalone entry points that all run outside Next.js's own automatic env loading — `prisma.config.ts`, `prisma/seed.ts`, `scripts/test-db.ts`, `scripts/delete-non-demo-users.ts` — each loading `.env` then re-loading `.env.local` with `override:true` so `.env.local` wins. Verified via Context7 against Node's current CLI/process docs plus live empirical tests (not just docs) that `process.loadEnvFile()` behaves differently from dotenv/the CLI `--env-file` flag in one load-bearing way: it does _not_ overwrite a key already present in `process.env` on a later call (confirmed live: calling it twice, second call's values were silently ignored) — the opposite of dotenv's `override:true`. Worked around this by reversing the load order instead of trying to force an override: call `loadEnvFile(".env.local")` first (wrapped in try/catch since the file is optional/gitignored and a missing file throws `ENOENT`, confirmed live), then unconditionally `loadEnvFile(".env")` second — since `loadEnvFile()` only fills in keys not already set, this reproduces the exact same `.env.local`-wins-over-`.env` precedence as the original dotenv code, confirmed live both with and without `.env.local` present. Ran `npm uninstall dotenv`; it remains only as an indirect transitive dependency of dev tooling (`shadcn`'s CLI, another dev dependency), not of this project's own code. Verified live end-to-end against the real dev Neon DB on the actual, uncontrived invocation paths rather than just unit-level checks: `npx prisma migrate status` (confirms `prisma.config.ts`'s `env("DATABASE_URL")` resolves correctly), `npm run db:test` (confirms `scripts/test-db.ts`), `npm run db:delete-non-demo-users` (dry-run, confirms `scripts/delete-non-demo-users.ts`), and `npx prisma db seed` (confirms `prisma/seed.ts`, invoked through `prisma.config.ts`'s `migrations.seed` command) — all succeeded and returned real seeded data. Hit and resolved a git-workflow snag along the way: this fix and the immediately-prior sanitize-filename fix were both edited uncommitted on the same branch, and since uncommitted changes aren't branch-scoped in git, plain `checkout`s kept recombining the two; resolved by committing/merging the sanitize-filename fix first (explicit user confirmation obtained), then using `git stash push -- <specific files>` to isolate this fix's files before branching off cleanly. No unit tests added — these are standalone scripts/config outside the `src/actions/`/`src/lib/` Vitest scope and outside Next.js's own request pipeline. `npm run test` (147/147, no regressions), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass.
- 2026-08-26: Upload Rate Limiting (user-requested) implemented on branch `feature/upload-rate-limiting`. `POST /api/upload` had no rate limiting despite the existing Upstash-backed `src/lib/rate-limit.ts` infra already covering all 5 auth endpoints; added an `upload` scope (20 requests / 1 hour, per explicit user choice over a tighter 10/hour option) to `RateLimitScope`/`LIMITS`, keyed by `session.user.id` rather than IP — the route already requires auth, and per-user is the more meaningful unit for storage-consuming uploads than a shared-IP key. Route now checks the limit right after the existing auth check and before touching `formData`/R2, returning the same `429` + `Retry-After` header + JSON `{success:false,error}` shape as `/api/auth/register`; the existing `checkRateLimit`/`rateLimitMessage`/`retryAfterSeconds` helpers needed no changes, and no new test cases were added since `rate-limit.test.ts`'s existing `checkRateLimit` fail-open test already exercises the scope-agnostic code path generically. `FileUpload.tsx`'s client already surfaces `body.error` on any non-200 response, so the 429 message needed no UI changes. Verified live end-to-end against the real dev Neon DB, real Upstash Redis, and real R2 bucket (not mocked): signed in as the demo user via Playwright, fired 21 real image uploads in a loop from the browser — the first 20 returned `200` with real R2 URLs, the 21st returned `429` with `Retry-After: 2093` and "Too many attempts. Please try again in 35 minutes."; all 20 real R2 test objects deleted afterward via a temp script (created and removed within this session) confirmed via per-object delete logs, 0 remaining. `npm run test` (147/147, no regressions), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass.
- 2026-08-26: Fix: distinct sign-in error for GitHub-only accounts, closing the remaining open follow-up from the 2026-07-30/08-03 Auth Phase 2 review, implemented on branch `fix/github-only-sign-in-error`. `authorize()` in `src/auth.ts` previously returned `null` (→ generic "Invalid email or password") both for an unknown email and for a real account with no password (GitHub-only) — split into two branches: unknown email still returns `null`, but a real user with `password === null` now throws a new `GitHubOnlyAccountError extends CredentialsSignin`, mirroring the existing `EmailNotVerifiedError`/`RateLimitedError` pattern. `signInWithCredentials` (`src/actions/auth.ts`) catches it and returns `"This account uses GitHub — sign in with GitHub instead."`; wrong password and unknown email are unaffected and still share the generic message. Explicit user choice offered up front (consistent with this project's established pattern for existence-revealing trade-offs, e.g. the resend-verification/forgot-password no-leak design): accept the minor enumeration trade-off — this reveals "this specific email is a GitHub-only account" but not whether an unrecognized email is registered at all, since wrong-password and unknown-email both stay generic — in exchange for real UX value for a user who forgot they signed up via GitHub. Added a `GitHubOnlyAccountError` test case to `src/actions/auth.test.ts` alongside the existing `EmailNotVerifiedError`/`RateLimitedError`/generic-`AuthError` cases — 148/148 total. Verified live against the real dev Neon DB and dev server: inserted a throwaway GitHub-only user directly (`password: null`, no linked `Account` row needed for this check) via Neon MCP, confirmed via curl against the raw `/api/auth/callback/credentials` endpoint that it returns `code=github-only-account`, then confirmed via Playwright that `/sign-in` renders the friendly message end-to-end through the real Server Action; separately confirmed an unknown email still shows the generic "Invalid email or password" with no behavior change. Test user deleted from the dev Neon DB afterward. `npm run test` (148/148), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass.
- 2026-08-26: Split `ItemDrawer.tsx`/`CreateItemDialog.tsx` into smaller pieces (per a `codebase-scanner` pass identifying them as the two largest components in the codebase) implemented on branch `feature/split-item-drawer-create-dialog`. Pure refactor, no behavior change. `ItemDrawer.tsx` (481→~155 lines) split into `ItemDrawerSkeleton.tsx`, `ItemDrawerActionBar.tsx` (view/edit action buttons + delete confirmation `AlertDialog`), `ItemDrawerViewContent.tsx`, `ItemDrawerEditForm.tsx`, and `ItemDrawerMetadata.tsx` (collection + created/updated dates — rendered in both modes, so kept outside the view/edit split). `CreateItemDialog.tsx` (274→~185 lines) split into `ItemTypeSelect.tsx` (the type dropdown with its icon-rendering `SelectValue` render-prop) and `CreateItemFields.tsx` (URL/content/file-upload conditional group, also now the home of the `CreateItemFormState` type). Deduped the `TYPES_WITH_CONTENT`/`TYPES_WITH_LANGUAGE`/`TYPES_WITH_URL`/`TYPES_WITH_CODE_EDITOR`/`TYPES_WITH_MARKDOWN_EDITOR`/`TYPES_WITH_FILE_UPLOAD` arrays — previously defined identically in both files — into `src/lib/item-type-capabilities.ts`, exposing them as `typeShows*(typeName: string)` functions rather than raw arrays since `ItemDrawer`'s `item.type.name` is a DB-sourced `string` while `CreateItemDialog`'s `form.type` is the narrower `ItemType` union; the helper casts internally so both call sites can pass a plain string without a type error. Moved `EditForm`/`toEditForm`/`formatDate` out of `ItemDrawer.tsx` into `src/lib/item-drawer-utils.ts`. Verified live via Playwright as the demo user: opened the `useDebounce Hook` snippet's drawer, confirmed view mode (description/content/collection/dates) and edit mode (title/description/content/language/tags all pre-filled) both render correctly across the new split components, added then removed a test tag to confirm the full edit→save round-trip still works and reverted the item to its original state afterward; opened the Create Item dialog and confirmed type-switching (Snippet→Link→Image) correctly toggles the URL field, content editor, and file-upload field via the split `CreateItemFields`/`ItemTypeSelect` components; no console errors throughout. `npm run test` (148/148, no regressions), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Ready to commit and merge.
- 2026-08-26: Split `src/lib/db/items.ts` (per this file's own spec, raised by a `codebase-scanner` pass) implemented on branch `feature/split-items-db-module`. Reads moved to `src/lib/db/items-queries.ts`, mutations to `src/lib/db/items-mutations.ts` (imports `getItemDetail` from `items-queries.ts` for `updateItem`'s post-write refresh), metadata to `src/lib/db/item-metadata.ts` — named to avoid a basename collision with the pre-existing `src/lib/item-types.ts` (UI constant module, unrelated), an explicit choice offered up front. Updated all ~20 consumer imports across `src/app/`, `src/components/`, `src/actions/items.ts`, and `src/types/items.ts`, splitting any that pulled from more than one of the new modules. Split the colocated `items.test.ts` into `items-queries.test.ts`/`items-mutations.test.ts`/`item-metadata.test.ts` to match, and split `actions/items.test.ts`'s single `vi.mock("@/lib/db/items")` into two mocks matching the new import paths. One small logic tweak bundled in at explicit user request while reviewing the split, not part of the original scope: in `deleteItem`, moved the `if (count > 0 && existing?.fileUrl)` check inside the `try` block wrapping the R2 delete (previously the `if` wrapped the `try`) — behaviorally identical since the condition itself can't throw, but a clearer read of what's actually being guarded. Verified live via Playwright as the demo user: `/dashboard` renders real sidebar type counts (from `item-metadata.ts`), stats cards, collections, and recent items (from `items-queries.ts`) with no console errors; opening the "Tailwind CSS Docs" item drawer correctly fetches and renders full detail (description/URL/collection) via `getItemDetail`; `/items/link` (using `getItemsByType`/`getItemTypeByName`) renders all 6 real links with no console errors. `npm run test` (148/148, no regressions), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Ready to commit and merge.
- 2026-08-27: Collection Create (inline spec, loaded rather than from a spec file) implemented on branch `feature/collection-create`. Added a "New Collection" button/modal in the top bar following the same patterns established for items: `createCollection` in `src/lib/db/collections.ts` (user-scoped via `getCurrentUserId()`), `createCollectionSchema` in `src/lib/validations/collections.ts`, and `POST /api/collections` (`src/app/api/collections/route.ts`) — per explicit spec direction, a client-facing API route rather than a Server Action, matching `POST /api/upload`'s shape. `CreateCollectionDialog.tsx` mirrors `CreateItemDialog.tsx`'s structure, scoped to name (required) + description (optional). A `/do-i-understand` session surfaced and closed one real gap before merge: the route's `createCollection` call had no `try/catch`, so a mid-request session loss would throw uncaught into a raw 500 instead of the app's `{success,error}` envelope; fixed by wrapping it (mirroring `GET /api/items/[id]`'s pattern) and having the dialog special-case a 401 with a "session expired" toast plus a redirect to `/sign-in?callbackUrl=...`. Verified live via Playwright: create/cancel flow, stats/sidebar/Recent Collections updating without a manual refresh, and the session-expiry fix confirmed end-to-end by clearing cookies mid-dialog and watching the redirect-then-sign-back-in round-trip land back on `/dashboard`. `npm run test` (152/152), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Committed and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-30/31: Item ↔ Collections multi-select (inline spec, loaded rather than from a spec file) implemented on branch `feature/item-collections-multi-select`. Replaced `Item`'s single optional `collectionId String?` FK with a many-to-many `ItemCollection` join table (mirroring the existing `ItemTag` pattern) — flagged up front as an architectural change before starting. Hand-wrote the migration SQL (`prisma migrate dev --create-only` couldn't run non-interactively in this environment) with the `ItemCollection` table/backfill `INSERT` ordered *before* the `DROP COLUMN "collectionId"`, so the 18 seeded items' existing associations were preserved (verified via direct DB query: 18 rows backfilled, 0 orphaned after); applied via `prisma migrate deploy` against the real dev `development` Neon branch after confirming the connection string matched via Neon MCP. Restructured `prisma/seed.ts`'s nested `items: { create: [...] }` under each collection to `items: { create: [{ item: { create: {...} } }] }` to match the new join-table relation shape. Added `getAllCollections()` (`src/lib/db/collections.ts`) and `getOwnedCollectionIds()` (`src/lib/db/items-mutations.ts`, a private helper) — the latter re-scopes any client-submitted `collectionIds` to ones the current user actually owns before linking, closing an IDOR gap where a client could otherwise splice an item into another user's collection by guessing its id; confirmed via this codebase's existing read paths (`getRecentCollections`/`getAllCollections`, both `userId`-scoped) that an unowned id slipping through would land as an inert orphaned row rather than a real leak, given no unscoped `/collections/[id]` page exists yet. Added `CollectionsMultiSelect.tsx` (a `DropdownMenu` + `DropdownMenuCheckboxItem` picker — checkbox items default `closeOnClick={false}`, so the menu stays open across multiple toggles) wired into both `CreateItemDialog.tsx` and `ItemDrawerEditForm.tsx` (pre-populated from `item.collections` on edit), plus `GET /api/collections` and `fetchCollectionOptions` (`src/lib/swr-fetcher.ts`) to fetch the picker's options client-side. `createItem`/`updateItem` (`src/lib/db/items-mutations.ts`) now accept `collectionIds: string[]`; `updateItem` replaces the join rows (`deleteMany` + re-`createMany`, matching the existing tag-replacement pattern) and `createItem` inserts them inside the same transaction. `ItemDetail.collection: {id,name}|null` became `collections: {id,name}[]`, updating `getItemDetail`'s query/mapping and every consumer (`ItemDrawerMetadata.tsx`, `items-mutations.ts`, tests). A `/do-i-understand` review (four regions: migration ordering, the IDOR guard's actual blast radius in this codebase, `createItem`'s early-return branch, and `fetchCollectionOptions`'s error handling) surfaced one real, unfixed gap live: `fetchCollectionOptions` swallowed fetch/parse errors into an empty array, so a failed `/api/collections` request rendered the multi-select as "No collections yet" — an affirmative, false claim about the account, indistinguishable from a genuinely empty one, and actively misleading mid-edit on an item that already has real collections. Fixed post-review: removed the swallow (now throws, matching `fetchItemDetail`'s pattern), added a `hasError` prop to `CollectionsMultiSelect` rendering a destructive-styled "Failed to load collections" (or "Failed to load collection names (N selected)" when `selectedIds` is non-empty, preserving the true count even when names can't be shown) instead. While fixing it, also caught and fixed an identical pre-existing bug in `fetchItemDetail` (`res.json()` called before the `res.ok` check, so a non-JSON error body — e.g. a raw 500 HTML page — threw a raw `SyntaxError` instead of surfacing `body.error`) — higher-stakes there since `ItemDrawer.tsx` displays `error.message` directly to the user, unlike the collections picker's boolean-only `hasError` check; fixed by checking `res.ok` first and gracefully falling back to a generic message via `.catch(() => null)` if the error body itself isn't valid JSON. Added `src/lib/swr-fetcher.test.ts` (8 tests: success, JSON error body, non-JSON error body, ok-but-`success:false`, for both functions) per this project's workflow for changes touching `src/lib/`. Also caught a UI bug independent of the review: `ItemDrawerMetadata.tsx`'s read-only Collections badges rendered unconditionally alongside the new editable picker, duplicating it in edit mode — fixed by adding a `showCollections` prop gated on `mode !== "edit"`. Verified live end-to-end via Playwright against the real dev Neon DB and demo user: created an item with 2 collections and confirmed via direct DB query; edited it to swap one collection for another and reconfirmed via DB query; deleted it and confirmed cascade cleanup left 0 orphaned `ItemCollection` rows; separately monkey-patched `window.fetch` to fail `/api/collections` and confirmed both the empty- and non-empty-selection error states render distinctly (screenshotted) from the genuine empty state. `npm run test` (163/163), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass throughout. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-08-31: Collections Pages (inline spec, loaded rather than from a spec file) implemented on branch `feature/collections-pages`. Added `/collections` (all of the current user's collections via new `getAllCollectionSummaries()`, sharing a `fetchCollectionSummaries` helper with the existing `getRecentCollections`) and `/collections/[id]` (a single collection's items via new `getCollectionById()` — ownership-scoped, no-existence-leak like `getItemDetail` — and `getItemsByCollection()`), both rendered through `DashboardShell`/`SidebarContainer` like the other dashboard routes. Added `CollectionItemsGrid.tsx`, splitting a collection's mixed-type items into `otherItems`/`imageItems`/`fileItems` buckets and rendering each through the existing `ItemCard`/`ImageCard`/`FileListItem`, mirroring `ItemsGrid.tsx`'s per-type grid classes; `otherItems` and `imageItems` share one `CardSection` helper (parameterized by className + a `renderItem` function) after an explicit request to de-duplicate two near-identical section components, while still rendering as separate DOM sections per explicit request (an earlier, more consolidated single-grid version had also been explicitly rejected). `CollectionCard.tsx` now wraps itself in a `Link` to `/collections/[id]`, so every place it renders (dashboard's Recent Collections and the new `/collections` page) is clickable; the sidebar's pre-existing "View all collections" link now resolves correctly. Added both new routes to `proxy.ts`'s protected matcher using an exact-match-or-trailing-slash `startsWith` check rather than a bare `startsWith("/collections")`, deliberately avoiding the same loose-prefix latent-collision shape the pre-existing `/dashboard`/`/profile` checks have (no colliding sibling route today, but not a pattern worth copying). Also linked the top bar's "DevStash" logo/text back to `/dashboard`, an explicit user request unrelated to the collections spec. A `/do-i-understand` session caught one real, pre-fix gap: `getItemsByCollection` (`src/lib/db/items-queries.ts`) filtered by `{ userId, collections: { some: { collectionId } } }` with no ownership check on `collectionId` itself, relying entirely on the page's separate `getCollectionById` call as the only gate — safe in the current app only because of an invariant enforced elsewhere (collection IDs get re-scoped to owned ones at item-create/update time, per the 2026-08-30/31 IDOR fix), not because this function verified anything itself. Author correctly diagnosed both the gap and the fix unprompted; closed by adding `collection: { userId }` into the same `where` clause so the function is self-contained and safe regardless of caller, matching `getItemDetail`'s pattern. A user asking "I can't add items to my collections it is greyout" mid-session turned out to be expected behavor, not a bug: `CollectionsMultiSelect` disables itself when the signed-in account has zero collections yet (a different, collection-less account, not the demo user) — resolved by explaining that creating a collection first populates the picker. Verified live via Playwright throughout: `/collections` lists all 5 real collections; a collection detail page renders its real items via the item drawer (confirmed `Collections: Design Resources` in the drawer's metadata) with no console errors; an invalid/unowned collection id shows "No such collection." instead of crashing; the sidebar link and the DevStash-logo link both navigate correctly. `npm run test` (163/163, no regressions), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-09-01: Collection Edit/Delete/Favorite Actions (inline spec, loaded rather than from a spec file) implemented on branch `feature/collection-edit-delete-favorite`. Added `updateCollection`/`deleteCollection` to `src/lib/db/collections.ts` and `updateCollectionSchema` to `src/lib/validations/collections.ts`, backing new `PATCH`/`DELETE /api/collections/[id]` routes (`src/app/api/collections/[id]/route.ts`), matching the existing client-facing-API-route convention from Collection Create rather than a Server Action. Added `EditCollectionDialog.tsx` (name/description modal, mirrors `CreateCollectionDialog.tsx`), `CollectionActionsMenu.tsx` (the cards' 3-dot `DropdownMenu` with Edit/Favorite/Delete), and `CollectionDetailActions.tsx` (the detail page's Favorite/Edit/Delete button row, mirroring `ItemDrawerActionBar.tsx`'s layout) — Favorite renders in both but is intentionally unwired (icon/state only) per spec. Restructured `CollectionCard.tsx`: the card's `Link` moved from wrapping the whole card to an absolutely-positioned, empty overlay (`absolute inset-0`) sitting *underneath* a `relative z-10` wrapper around the actions menu, so the dropdown no longer triggers navigation while the rest of the card still does — author correctly traced the CSS stacking mechanics (position + z-index required together; without them the positioned Link paints above the static `CardContent` and swallows clicks) when interrogated. At explicit user request mid-session, adopted `useSWRMutation` (`swr/mutation`, not previously used in this codebase) for both the edit-save and delete calls via new `updateCollectionMutation`/`deleteCollectionMutation` fetchers and an `ApiError` class in `src/lib/swr-fetcher.ts`; after the same 401-redirect-to-sign-in-with-toast block appeared duplicated across three components, extracted it into `src/hooks/use-api-error-toast.ts` (`useApiErrorToast`) — this project's first `src/hooks/` file. Several design points were pushed back on before implementation and resolved via `AskUserQuestion` rather than silently applied: kept ownership-scoping delegated to `updateCollection`/`deleteCollection` (via `getCurrentUserId()`-scoped Prisma `where` clauses) rather than re-deriving `userId` explicitly in the route, matching every other `db/*` function in this codebase; kept a flat 404 (not 403) for both "doesn't exist" and "exists but isn't yours", matching `getItemDetail`/`GET /api/items/[id]`'s established no-existence-leak convention; kept the existing `err.message === "Not authenticated"` string-match convention over introducing a typed error class, since the latter would require refactoring every existing call site project-wide. Two real gaps were caught and fixed before the AskUserQuestion rounds settled: the route's `updateCollection`/`deleteCollection` calls had no `try/catch` (mirroring the exact bug the 2026-08-27 Collection Create feature had already fixed once), and `PATCH`'s `request.json()` was unguarded against a malformed body — both fixed. A claimed "malformed id could 500" risk was checked empirically rather than argued about: a temp script (`scripts/tmp-id-check.ts`, deleted after) confirmed against the real dev Neon DB that `findFirst`/`deleteMany` with a garbage `id` string return `null`/`{count:0}` without throwing, since `Collection.id` is a plain `String @id @default(cuid())` column, not a native Postgres `uuid` type — no fix needed. A `/do-i-understand` session (three regions: the 404-vs-403 design, an inconsistency between `updateCollection`'s check-then-`update` and `deleteCollection`'s atomic `deleteMany`, and the `CollectionCard` z-10 mechanism) surfaced one real, unfixed gap: `updateCollection`'s ownership check had no self-contained protection — the `update()` call itself carried no `userId` filter, so its safety depended entirely on the preceding `findFirst` check staying adjacent to it, unlike `deleteCollection`'s atomic query-level scoping; a future refactor could silently separate them with no test catching it. Fixed post-review at explicit request: rewrote `updateCollection` to `updateMany({ where: { id, userId }, data })` + refetch, matching `deleteCollection`'s shape; tests updated to match. Verified live end-to-end multiple times via Playwright against the real dev Neon DB and demo user, including after the atomic-update rewrite: edit modal pre-fills and saves (round-tripped on the real "Design Resources" collection, reverted after each test including the atomic-rewrite re-verification); delete confirmation's Cancel is a no-op; confirming Delete on a throwaway test collection (with a throwaway test item assigned to it) removed the collection and redirected to `/collections`, while the item itself survived untouched (confirmed via the sidebar's snippet count and `/items/snippet`, then cleaned up separately via the item drawer's existing delete); the 3-dot dropdown opens without navigating and lists Edit/Favorite/Delete; no console errors throughout. All test artifacts (collection, item) were created and deleted during verification; seed data was untouched. `npm run test` (178/178), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass throughout. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-09-01: Global Search / Command Palette (per `context/features/global-search-spec.md`) implemented on branch `feature/global-search`. Installed shadcn's `Command`/`InputGroup` (`src/components/ui/command.tsx`, `input-group.tsx`, pulling in `cmdk`). Added `getSearchableItems()` to `src/lib/db/items-queries.ts` — a genuinely new function (a deliberate, named exception to the spec's "reuse existing data fetching functions" note, since no existing function returns *all* items across types with a content preview: `getRecentItems` caps at 10, `getItemsByType` is per-type) — capped at 500 items (`take: 501`, slice + `truncated` flag) with a content-preview fallback chain (`content` → `description` → `url`, correctly skipping empty/whitespace-only strings, truncated at 140 chars). Reused the existing `getAllCollectionSummaries()` for collections rather than adding a narrower query. Added `GlobalSearchContext.tsx` (open-state provider + `KeyK`/repeat-guarded Cmd+K listener), `GlobalSearchDialog.tsx` (the cmdk UI: grouped Items/Collections `CommandGroup`s, type icons via the existing `iconMap`, per-item content previews as `keywords` for fuzzy matching, distinct empty-states for no-data/no-matches/load-failure, a truncation notice), and `GlobalSearchContainer.tsx` (async server component fetching both sources). Wired `DashboardShell.tsx` with a new required `search` ReactNode prop (mirroring the existing `sidebar` prop pattern) wrapped in `GlobalSearchProvider`, with `{search}` rendered inside `ItemDrawerProvider` so `GlobalSearchDialog` can call `useItemDrawer()` to open items directly; collection selection navigates via `router.push`. Updated all 6 pages using `DashboardShell` to pass `search={<GlobalSearchContainer />}`. TopBar's search input opens the palette on click (existing `⌘K` `<kbd>` badge reused as the hint, not literally embedded in the `placeholder` string — a minor deviation from the spec's literal wording, functionally equivalent). Found and fixed two real bugs during live verification: (1) the freshly-installed shadcn `CommandDialog` rendered `DialogHeader` as a sibling of `DialogContent` instead of a child, breaking `aria-labelledby`/`aria-describedby` wiring — fixed by nesting it inside, with a comment flagging the position as load-bearing since the exact mechanism connecting it to the Escape/outside-click symptoms observed during debugging was never fully root-caused (traced into Base UI's actual `useDismiss` source via the installed `node_modules` code — confirmed `closeOnEscapeKeyDown` has no DOM-containment dependency, so the earlier "layer stack"/"portal" theories didn't hold up; left as a confirmed fix with an honestly-labeled unknown mechanism rather than a fabricated explanation); (2) TopBar's search input originally had both `onClick` and `onFocus` opening the palette, which raced with the dialog's outside-click dismissal (opening mid-gesture on focus caused the same click's mouseup to register as an outside press) — fixed by removing the redundant `onFocus`. A `/do-i-understand` session pressed on five regions and, live, talked through upgrading `GlobalSearchContainer` from `Promise.all` + one combined `try/catch` to `Promise.allSettled` with independent `itemsError`/`collectionsError` flags (author explicitly confirmed this design hadn't been deliberated before being asked) — implemented afterward with distinct per-section failure messaging (`GlobalSearchDialog` now shows "Couldn't load items — showing collections only." / "Couldn't load collections — showing items only." rather than blanking both on a single failure), each rejection logged separately via `console.error`. Added 5 unit tests for `getSearchableItems()` (`items-queries.test.ts`: user-scoping, the content-preview fallback chain including the empty-string/whitespace edge case, truncation, and the `truncated` flag both under and over the cap) — 183/183 total; no tests added for the new components per this project's existing Vitest scope (`src/actions/`/`src/lib/` only). Verified live via Playwright throughout: Cmd+K open/close, click-to-open, Escape and outside-click dismissal (post-fix), fuzzy search including typo tolerance ("tailwnd" → "Tailwind CSS Docs"), Enter-to-select opening the item drawer, clicking a collection result navigating to `/collections/[id]`, and no console errors — across all 6 pages using `DashboardShell`. `npm run test` (183/183), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted.
- 2026-09-02: Settings Page (inline spec, loaded rather than from a spec file) implemented on branch `feature/settings-page`. Added a protected `/settings` route (`proxy.ts`'s middleware check and matcher both updated, same `startsWith` pattern as `/profile`) and a "Settings" link in the sidebar's user-icon dropdown (`Sidebar.tsx`), next to the existing "View Profile" entry. The `AccountActions` card (change password + delete account) moved from `/profile` to the new `src/app/settings/page.tsx`; `/profile` keeps only `ProfileInfo`/`ProfileStats`. Resolved a spec ambiguity up front via `AskUserQuestion`: the request named "delete account and forgot password" as what's moving, but the existing `AccountActions` had no forgot-password entry point (only change-password + delete-account) — confirmed the user wanted a new "Forgot password?" link to the existing `/forgot-password` flow added alongside the move, not just the move alone. Along the way, pushed back three times on my own draft before the user accepted it: confirmed via Context7 that NextAuth's default `redirect` callback already blocks open-redirect `callbackUrl` values in this app (no `auth.ts` customization needed, so the `/settings` matcher addition was safe as originally written); rewrote the settings page's error handling twice at explicit request — first to stop rendering raw `err.message` (Prisma internals were leaking to the client), then to redirect to `/sign-in?callbackUrl=/settings` on an auth-specific failure instead of rendering an error paragraph on a page the user shouldn't see (session expired/revoked mid-request bypasses `proxy.ts`, which only checks the cookie); and replaced an initial hardcoded-margin centering trick (copied from `/profile`'s existing `mr-[490px]`/`mr-[400px]` pattern) with a width-matched wrapper instead, after the user flagged the margin approach as fragile — coupled to `AccountActions`' card width silently breaking if it ever changes. Mid-session, the user made several manual edits directly to `AccountActions.tsx` (replacing the "Forgot password?" `Link` with a static, non-navigable `<p>`, among other layout tweaks) that a `/feature review` pass caught before merge: flagged the regression (the confirmed forgot-password-link goal was no longer functional — no `href`, reports as `paragraph` not `link` in the accessibility tree), the resulting unused `Link` import (lint warning), and the unexplained `ml-[-150px]` magic-number positioning hack. Offered to fix before completing; user explicitly chose to complete as-is. **Follow-up still open, left unfixed at explicit user request:** `AccountActions.tsx`'s "Forgot password ?" text is not a working link — needs to go back to `<Link href="/forgot-password">` (also clears the unused-import lint warning) in a future session. Verified live via Playwright throughout (aside from the known Forgot Password regression): unauthenticated `/settings` redirects to sign-in with `callbackUrl` (curl-confirmed 307); sidebar dropdown → Settings navigates correctly at both mobile-drawer and desktop-sidebar widths; Change Password dialog opens and functions from the new location; `/profile` correctly no longer renders Account actions; header/card alignment confirmed at 375px/1400px widths with no console errors. `npm run test` (223/223, no regressions), `npm run lint` (2 warnings: the pre-existing unrelated `JWT` one, plus the new unused `Link` import), `npm run build`, and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-09-02: Pagination (per `context/features/pagination-spec.md`) implemented on branch `feature/pagination`. Resolved a spec ambiguity up front via explicit user confirmation: the spec defined `COLLECTIONS_PER_PAGE` but its "Add pagination to" line only named `/items/[type]` and `/collections/[id]` (both item-listing pages) — confirmed this was a spec typo and `COLLECTIONS_PER_PAGE` was meant for `/collections` (the all-collections listing, previously unpaginated); also confirmed `/collections/[id]`'s three type-sectioned grids (other/image/file) should paginate as one combined 21-item page split across sections, rather than each section paginating independently. Added `src/lib/pagination.ts`: `ITEMS_PER_PAGE`/`COLLECTIONS_PER_PAGE` (21) and `DASHBOARD_COLLECTIONS_LIMIT`/`DASHBOARD_RECENT_ITEMS_LIMIT` (6/10) constants, plus `parsePageParam`/`getTotalPages`/`clampPage`/`getPaginationRange` helpers. `getItemsByType`/`getItemsByCollection` (`items-queries.ts`) and a new `getCollectionsPage` (`collections.ts`) now count, clamp the requested page to the real last page, then fetch only that page via `skip`/`take` — deliberately left `getAllCollectionSummaries`/`getSearchableItems` (used by global search) as unpaginated full-fetches, unrelated to this feature. Added a stable `id` secondary sort to all three paginated queries alongside the existing `createdAt`/`updatedAt` ordering, since same-timestamp ties (bulk import, fast successive creates) would otherwise have undefined order across separate `skip`/`take` requests and could leak an item onto two pages or none. Added `PaginationControls.tsx` (numbered links + prev/next, installed shadcn's `pagination` primitive but built custom `next/link`-based boundary links instead of reusing its `PaginationLink`/`Previous`/`Next` exports, which hardcode a bare `<a>`) — disabled prev/next render as a non-interactive `<span>` with no `href`, not just dimmed styling. `getRecentCollections`/`getRecentItems` defaults now reference the dashboard-limit constants instead of magic numbers. An iterative review process (the user rejecting each test/implementation attempt with a specific gap) hardened the design well beyond the first pass: `clampPage` was rewritten to sanitize non-integer/NaN input itself rather than trust callers to pre-parse with `parsePageParam`; unit tests were strengthened so "clamps beyond the last page" assertions use a count that actually produces multiple pages (a broken clamp that always returns 1 would have passed the original, weaker version); added explicit test coverage for zero-count (avoiding `Math.ceil(0/21)` producing a negative skip), below-1 requested pages, explicit `orderBy` assertions (previously hidden inside `objectContaining`), and session-failure propagation (a rejected `getCurrentUserId()` must not silently resolve to an empty page). Final count: 223 tests, up from 178 before this feature. Verified live via Playwright against the real dev Neon DB: seeded 25 throwaway snippets, 25 throwaway collections, and one 25-item collection (via a temp script, deleted after) to push all three paginated surfaces past their 21-per-page threshold, confirmed correct page counts, prev/next disabling at both boundaries, and `?page=999` clamping to the real last page (not a blank page) on `/items/snippet`, `/collections`, and the test collection's detail page; deleted all test data afterward and confirmed the demo account's counts and pagination-free state were restored. `npm run test` (223/223), `npm run build`, `npm run lint` (only the pre-existing, unrelated `JWT` warning), and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted.
- 2026-09-02: Editor Preferences Settings (per `context/features/editor-settings-spec.md`) implemented on branch `feature/editor-preferences-settings`. Added `editorPreferences Json?` to the `User` model via a real migration (`20260902200600_add_editor_preferences`, `prisma migrate dev`, never `db push`), `src/lib/editor-preferences.ts` (font size / tab size / theme option constants and `DEFAULT_EDITOR_PREFERENCES`), and `src/lib/validations/editor-preferences.ts` (full + `.partial()` Zod schemas). `src/lib/db/editor-preferences.ts`'s `getEditorPreferences`/`updateEditorPreferences` throw a typed `EditorPreferencesError` (checked via `instanceof`, mirroring the existing `EmailNotVerifiedError`/`RateLimitedError`/`GitHubOnlyAccountError` pattern in `auth.ts`) rather than string-matching error messages, distinguish "never saved" (`null`, silent) from "saved but unparseable" (logged) stored values, and merge a partial update against the live-fetched current value via an atomic `updateMany` ownership check — filtering out any stray `undefined` keys first, since Zod's `.partial()` keeps an explicit `{ theme: undefined }` as its own key rather than stripping it, which would otherwise let a naive spread overwrite a real stored value. `src/actions/editor-preferences.ts` (server action for writes) and `GET /api/editor-preferences` (route for the initial client-side read, matching the `GET /api/items/[id]`+`updateItem`-action precedent rather than the fully-route-based Collections pattern) both wrap their `auth()` call in the same try/catch as the rest of the body, so an `auth()` failure itself returns a result instead of throwing — a small hardening beyond this project's existing convention (other actions call `auth()` unguarded at the top), added specifically for this file and covered by a test. `EditorPreferencesContext.tsx` (SWR-backed reads via `fetchEditorPreferences` in `swr-fetcher.ts`, optimistic updates with `rollbackOnError` reading the *live* SWR cache rather than a stale render-closure value, so a second in-flight change failing can't roll back over a first change that already succeeded) is mounted at the top of `DashboardShell` — initially placed inside `<main>` only, which would have crashed `useEditorPreferences()` on `TopBar`'s "New Item" dialog (`CreateItemDialog`, which also renders `CodeEditor`) since `TopBar` sits outside `<main>`; caught and fixed before merge. `EditorPreferencesSettings.tsx` (new `src/components/settings/`) adds font size / tab size / theme `Select`s and word-wrap / minimap `Switch`es (freshly-installed shadcn `Switch`) to `/settings`, auto-saving on change via `updatePreference` with a `sonner` success/error toast (a fixed toast `id` so rapid changes replace rather than stack). `CodeEditor.tsx` now reads `useEditorPreferences()` for `fontSize`/`tabSize`/`wordWrap`/`minimap`/`theme`, and registers `monokai`/`github-dark` (approximated palettes; Monaco only ships `vs`/`vs-dark`/`hc-black`) via a `beforeMount`/`monaco.editor.defineTheme` handler, confirmed via Context7 against `@monaco-editor/react`'s docs. Added 33 new unit tests across `src/lib/validations/`, `src/lib/db/`, and `src/actions/` (partial-schema unknown-key stripping and explicit-`undefined` behavior, the db layer's merge-onto-current/defined-only-write logic, ownership-miss and session-failure propagation, and the action's `instanceof`-not-string-match error mapping, including a case using a plain `Error("Account not found")` to prove the check isn't string-based) — 256/256 total. Verified live via Playwright against the real dev Neon DB: settings persist across a fresh page load, the Monaco editor in both the item drawer and the top-bar create-item dialog visibly reflects the saved Monokai theme and enabled minimap with no console errors; demo user's `editorPreferences` reset to `null` afterward. `npm run test` (256/256), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-09-03: Favorites Page (per `context/features/favorites-spec.md`) implemented on branch `feature/favorites-page`. Added `getFavoriteItems()` (`src/lib/db/items-queries.ts`) and `getFavoriteCollections()` (`src/lib/db/collections.ts`), both scoped to the current user, `isFavorite: true`, ordered by `updatedAt desc` (a proxy for "most recently favorited" since there's no dedicated `favoritedAt` field). A `/feature review` pass caught a real discrepancy before merge: the spec listed "date added" (row display) and "sort by updatedAt" (ordering) as two separate requirements, but the initial implementation reused `updatedAt` for both — fixed by selecting/displaying `createdAt` in the row instead, keeping `updatedAt` purely as the sort key. Added `FavoritesList.tsx` (client component: compact monospace list, separate "Items (N)"/"Collections (N)" sections each with independent error state via `Promise.allSettled`, matching the `GlobalSearchContainer` precedent; item rows open `ItemDrawer`, collection rows link to `/collections/[id]`; single empty state when both are empty) and `favorites/page.tsx`, which redirects to sign-in on an auth-specific failure (session expired mid-request bypasses `proxy.ts`, which only checks the cookie) rather than showing a raw error — matching `settings/page.tsx`'s established pattern, confirmed via `AskUserQuestion` to keep the existing `err.message === "Not authenticated"` string-match convention over introducing a typed error class. Added a star icon button to `TopBar.tsx` linking to `/favorites`, and protected the route in `proxy.ts`. Added 6 new unit tests across `items-queries.test.ts`/`collections.test.ts` (query shape including explicit `select`, mapping, session-failure propagation) — 262/262 total. Verified live via Playwright as the demo user: 6 favorite items and 2 favorite collections render correctly with real dates/badges, item-row click opens the drawer with correct data, collection-row click navigates to its detail page, no console errors. Separately, mid-session, fixed two unrelated bugs at explicit user request, committed separately: (1) `CodeEditor.tsx`/`MarkdownEditor.tsx`'s expanded (Maximize2) modal showed the default `DialogContent` background/ring/padding around the editor's own already-styled window box, making it look like two nested frames — removed via `bg-transparent p-0 ring-0`; (2) `MarkdownEditor.tsx`'s Write panel rendered ~5px taller than Preview at the same content — root-caused to `<textarea>`'s default `display: inline-block` reserving line-box space for the font's descender when wrapped in a block container (Tailwind's preflight doesn't reset form controls to `block`), unlike Preview's div-based height; fixed by adding `block` to the textarea and matching `minHeight` on the Preview panel, confirmed via direct DOM measurement (wrapper height went from consistently 5px over the textarea to an exact match). Also attempted and fully reverted, at explicit user request, a Tab-key toggle for MarkdownEditor's Write/Preview tabs: three iterations (keydown-redirect trapped forward Tab in an infinite toggle; an `onFocus`-based rewrite didn't work at all since Base UI's roving tabindex means the non-selected trigger is never a real Tab stop; a "toggle once per visit" version fixed both directions but was found, via a full realistic keyboard-only "fill out a new prompt" walkthrough, to hijack the natural Tab-through-the-form flow — diverting a user tabbing from Description toward the Write textarea into Preview instead) before deciding the conflict between Tab-as-navigation and Tab-as-toggle wasn't resolvable cleanly enough to keep; reverted to plain Base UI behavior (Tab navigates, click/arrow-keys switch tabs). `npm run test` (262/262), `npm run lint` (only the pre-existing, unrelated `JWT` warning), `npm run build`, and `tsc --noEmit` all pass throughout. Committed as two focused commits (the favorites feature, then the separate editor fixes) and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
- 2026-09-03: Favorite Toggle (Drawer, Collection Page, Cards) (inline spec, loaded rather than from a spec file) implemented on branch `feature/favorite-toggle`. Wired the three previously-static Favorite buttons that already existed with correct markup but zero click handling: `ItemDrawerActionBar.tsx`, `CollectionDetailActions.tsx`, and `CollectionActionsMenu.tsx`'s dropdown item. Backend was missing end-to-end for both entities: added `setItemFavorite`/`setCollectionFavorite` DB mutations (ownership-scoped atomic `updateMany`, log-and-rethrow on failure, refetch via the existing `getItemDetail`/`getCollectionById`), a new `toggleItemFavorite` server action in `src/actions/items.ts` (items are mutated via server actions in this codebase), and a new `PATCH /api/collections/[id]/favorite` route + `toggleCollectionFavoriteSchema` (collections are mutated via client-facing API routes) — plus a shared `ToggleCollectionFavoriteResponse` type and `toggleCollectionFavoriteMutation` fetcher. Added clickable favorite toggles (previously passive `isFavorite` badges) to `ItemCard`, `ImageCard`, `ItemRow`, `FileListItem`, and `CollectionCard`, via a new shared `FavoriteToggleButton` presentational component and two new hooks, `useToggleItemFavorite`/`useToggleCollectionFavorite` (optimistic flip-then-revert-on-failure, a re-entrancy guard against rapid double-clicks, and an adjust-during-render resync so a stale `useState`-seeded value doesn't linger when the same item/collection is shown in two places at once and one of them refreshes via `router.refresh()`). Iterated through several rounds of inline review during implementation: added try/catch/log-and-rethrow to the two new DB mutations after being asked why they lacked it (matching `deleteItem`'s R2-cleanup precedent, not the try-catch-free `updateItem`/`updateCollection`); unified three different "toggle a boolean and sync the UI" patterns that had drifted across the drawer (SWR cache) and the two new hooks (local state) onto one consistent optimistic-flip-with-revert contract, including rewriting `ItemDrawer.tsx`'s `handleToggleFavorite` to match; retrofitted the same try/catch hardening onto `createItem`/`updateItem`/`deleteItem` in `src/actions/items.ts` (not otherwise part of this feature) after reasoning that leaving only the new `toggleItemFavorite` consistent while its three siblings in the same file diverged would be a worse inconsistency than the added scope; and replaced an unchecked `as CollectionDetail` cast in the new `toggleCollectionFavoriteMutation` fetcher with a minimal shared `{success,error}` response type once it was pointed out nothing downstream actually consumed the cast data. A `/do-i-understand` session covered five regions (ownership scoping on the new writes, the adjust-during-render resync pattern, an asymmetry where the DB mutations' `try/catch` wrapped the write but not the post-write refetch, the scope of the action-layer retrofit, and a `relative z-10` CSS-stacking dependency in `CollectionCard.tsx` that keeps the new favorite button clickable underneath the card's full-bleed `Link` overlay) — all substantively accounted for, and the refetch-asymmetry discussion led to two live fixes (wrapping the refetch in `items-mutations.ts` and its sibling in `collections.ts`) made mid-review rather than merged as flagged follow-ups; author correctly reasoned through the consequence (a refetch failure means the write already succeeded even though the user is told it failed) before making the fix. No named blind spot from the closing question. Verified live via Playwright as the demo user across every surface, using the seeded demo data with each toggle immediately reverted afterward (0 net change to demo state): dashboard `ItemRow`/`CollectionCard` toggles with the stats cards (Favorite Items 6↔7, Favorite Collections 2↔3) updating via `router.refresh()`; the item drawer's Favorite button confirmed via the same stats sync; the collection detail page's `CollectionDetailActions` Favorite button; `CollectionActionsMenu`'s dropdown Favorite item; and cross-surface reflection confirmed directly by toggling from the drawer while sitting on `/favorites` and watching the row disappear/reappear (Items 6→5→6) without a manual reload. `ImageCard`/`FileListItem` were not live-tested (no seeded image/file items) but share the identical hook/button wiring already verified elsewhere. `npm run test` (284/284, up from 262), `npm run lint` (only the pre-existing, unrelated `JWT`/`XIcon` warnings), `npm run build`, and `tsc --noEmit` all pass. Committed as a single commit and merged into `main`; branch deleted (never pushed to origin, so no remote cleanup needed).
