// Shared width for the /settings and /profile page columns and cards, so
// they all stay the same size (mobile: 300px, tablet: 700px, desktop:
// 790px) without each usage re-typing the same breakpoint triplet.
export const SETTINGS_CARD_WIDTH = "w-[300px] md:w-[700px] lg:w-[790px]";
